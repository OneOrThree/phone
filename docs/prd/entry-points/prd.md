# 세션 시작 진입점 확장 (Entry Points) — PRD

> `docs/prd/apple-watch/policy.md` **D4**로 애플워치 PRD에서 분리된 자매 문서다. 헤드리스 세션 엔진(같은 문서 D2)이 공통 선행 조건이며, 착수 시점은 그 엔진과 워치 코어(페이즈 1) 이후다. 이 문서는 초안 골격이다 — 착수 전 자체 policy.md와 함께 살을 붙인다.

## 1. 배경

집중 세션의 시작은 현재 「앱 열기 → 카테고리 화면 → 과목 선택 → 시작」 한 경로뿐이다. 애플워치 PRD가 손목 진입점을 만들었듯, 폰 쪽에도 앱을 열지 않는 진입점 두 개를 추가한다. 두 기능 모두 워치와 무관한 폰 표면이라 별도 트랙으로 진행한다.

## 2. 범위

| # | 요구사항 (애플워치 PRD에서 이관) | 비고 |
|---|---|---|
| E1 (구 R14) | 홈스크린 위젯: 오늘 집중 시간 표시 + **세션 시작 버튼은 App Intent 기반 인터랙티브 위젯(iOS 17+)으로 앱을 열지 않고 시작**한다 (딥링크로 앱을 여는 구현은 이 문서의 목적 미달 — E2와 같은 영속 명령 계층을 호출). 통계 영역 탭만 앱 딥링크 | 기존 `com.oneorthree.gromo.widget` App ID 재사용 — 발급 0. **현행 `Widget.swift`는 정적 템플릿이라 「활성화」가 아니라 신규 구현**이다 — 오늘 누적의 App Group export부터 만들어야 함 |
| E2 (구 R15) | 「집중 시작」 App Intent: Siri·단축어·잠금화면 위젯에서 같은 인텐트로 세션 시작 | **앱을 열지 않는 시작이 목적이므로 인텐트는 네이티브에서 영속 명령 계층(애플워치 PRD §4.1)을 직접 호출**해야 한다 — 앱을 여는 구현은 목적 미달. 별도 entitlement 없음 — 발급 0 |
| E3 (신규) | **최근 과목의 영속 원천 신설**: 세션 시작이 성공할 때마다 계정별 「최근 사용 과목 ID」를 영속 저장하고, 해당 과목 삭제·로그아웃 시 정리한다 | D10의 기본값(최근 과목)이 콜드 스타트에서도 결정 가능해야 하는데, **현재 저장소에 이 원천이 없다** — `FocusCategoryScreen`의 `selectedId`는 화면 로컬 상태고 `STORAGE_KEYS`에도 최근 과목 키가 없다. E1·E2 공통 선행. 네이티브(인텐트)에서 읽어야 하므로 저장 위치는 App Group 접근 가능해야 함. **기존 사용자 콜드 스타트**: 새 버전 설치 직후엔 이 값이 없다 — 출시 마이그레이션에서 기존 세션 기록·과목 선택 캐시로 1회 백필하고, 백필도 불가하면(기록 없음) 과목 미지정 인텐트는 조용히 실패하는 대신 **과목 선택을 요구하는 명시적 흐름**으로 빠진다 — 상세는 착수 시 LLD |

## 3. 이미 결정된 것 (정본: `docs/prd/apple-watch/policy.md`)

- **D10 (개정 반영)**: 인텐트 과목 미지정 시작의 기본값 = **최근 과목 자동**. ~~완료 후 변경 허용~~ — 이 보장은 D10 개정으로 **철회**됐다(완료 세션 과목을 바꿀 UI·API가 현재 없음). 오귀속 대응은 §4의 미결 항목으로.
- **D2**: 공통 선행 조건 = 영속 세션 상태 머신 + 정산 저널 + 헤드리스 엔진 + 네이티브 명령 인박스.
- 계측 계약(애플워치 PRD §5): 이 진입점들의 시작은 `start_origin='widget' | 'intent'`로 구분한다.

## 4. 미결 (착수 시 이 문서의 policy.md로)

- 위젯 크기·표시 정보의 우선순위 (오늘 누적 vs 바로 시작 버튼)
- 인텐트의 세션 완료/취소 지원 범위 (시작만? 제어 전부?)
- 자동화(단축어 트리거) 발 세션의 계측·이상 패턴 감시 기준
- **오귀속(잘못 붙은 최근 과목) 대응** — apple-watch policy **D10 개정** 참조. 후보: ⓐ 완료 세션 재귀속 UI+API(서버 변경) ⓑ 시작 직후 LA·알림에 과목 표시로 즉시 인지 ⓒ 시작 전 과목 확정. 현재는 완료 세션 과목 변경 수단이 UI·API 어디에도 없다
