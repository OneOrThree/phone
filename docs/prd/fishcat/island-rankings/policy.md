# 랭킹 정책·결정 기록

> **2026-09-21 개정(GROMO-1997 구현)** — 두 가지가 바뀌었다.
> ① **주민 랭킹 엔드포인트 폐지**(2026-09-16 결정 B23·B15): `GET /islands/{islandId}/rankings/members` 는
> 화면과 함께 사라졌다. 기획 정본도 「전망대는… **우리 섬 내부 주민 랭킹은 제공하지 않는다**」라고 적는다.
> 남은 계약은 섬 간 랭킹 `GET /rankings/islands` 하나뿐이다.
> ② **주 경계 = UTC 일요일**(2026-09-21 재영님 확정). 종전 RK-P01 의 「KST 월요일 + ISO week-year」는 폐기다.

| ID | 규칙 | 출처·상태 |
| --- | --- | --- |
| RK-P01 | ~~주는 KST 월요일00:00 포함~다음 월요일00:00 제외, ISO week-year 파싱~~ → **주는 UTC 일요일 00:00Z 포함 ~ 다음 일요일 00:00Z 제외**. 요일은 기획 정본 「주간 랭킹은 매주 일요일 00시에 초기화한다」, 존은 결정 D8(모든 시간 UTC)과 같은 축의 신규 기능 규칙(섬 퀘스트 Q-6·회관 기록 RC-축·집중 적립 D5-적립이 모두 UTC) | 2026-09-21 확정. 구현 `RankingWeek` |
| RK-P01-키 | **주 식별자는 ISO `YYYY-Www` 가 아니라 주 시작일 `YYYY-MM-DD`(UTC 일요일)다.** ISO 주차는 «월요일 시작»이 정의의 일부라, 요일만 일요일로 바꾸고 이름을 그대로 두면 `2026-W37` 이 ISO 가 말하는 7일과 다른 7일을 가리킨다 — 앱·서버·로그가 같은 문자열로 서로 다른 주를 뜻하게 된다. 주 시작일은 자기 자신이 경계를 말하므로 해석 규칙이 없고, ISO 의 「53주차가 없는 해」 같은 달력 예외도 생기지 않는다 | 2026-09-21 구현 결정(GROMO-1997). 검증 `RankingWeekTest` |
| RK-P02 | ~~주민 랭킹은 활성 소속·전망대·최소2명~~ → **폐지**(B23·B15). 섬 간 랭킹은 요청자의 **현재 섬**(`user_island_contexts.current_island_id`)이 살아 있고, 그 섬의 활성 주민이며, 그 섬의 **전망대가 완공**됐을 때 연다 | 확정. 「최소 2명」은 주민 랭킹 조건이었으므로 함께 사라진다 |
| RK-P03 | 1인 순위 잠금과 섬 발견은 별도 권한 | 원본. 발견 endpoint 를 랭킹 eligibility 로 차단하지 않음 |
| RK-P04 | 휴식은 집중 점수에 더하지 않는다. ACTIVE 구간만 센다 | PR743. 진행분 포함 여부는 RK-D03 에서 확정됨(아래) |
| RK-P05 | `data.asOf` 추가(승인된 기술 확장) | 승인된 기술 확장 |
| RK-P06 | rank/items/myRank/분모/표시 점수는 **같은 관측**에서 나온다 — 한 REPEATABLE READ 트랜잭션이 분자·분모를 함께 읽는다 | 기술 결정 |
| RK-P07 | ~~cursor15분 만료~~ → **이 계약에는 커서가 없다**(RK-D07). 들어온 커서는 400 `INVALID_CURSOR` | 2026-09-21 구현 결정 |
| RK-P08 | 섬 이름은 공개 projection. **개인정보를 싣지 않는다** — 사용자 이름·catColor·개인 점수가 응답에 없다 | 원본 공개 DTO 최소화. 주민 랭킹 폐지로 자연히 성립 |
| RK-P09 | ~~Data snapshot 생성/반환과 중앙 withdraw 를 공통 lifecycle 잠금으로 직렬화, 전 사용자 역색인~~ → **불필요**(RK-D08). 그 장치는 「표에 복사된 타인 PII」를 지키려는 것인데 이 응답에는 타인 PII 가 없다 | 2026-09-21 구현 결정. 선례 2026-09-19 RC-P12-적용 「복사본이 없으면 필요 없다」 |

## 결정된 것 (2026-09-21)

| ID | 결정 | 근거 |
| --- | --- | --- |
| RK-D01 | **분모 = 그 섬 «전체 주민 수»**(활성 멤버십 + 미탈퇴 계정 — `countByGroupIdIn` 과 같은 모수). **산정 시점은 「주가 끝난 시점」으로 동결**한다. 진행 중인 주는 아직 동결할 것이 없어 지금 인원으로 나눈다 | 기획 정본 「평균 = 해당 섬에서 집중한 시간 합계 ÷ 해당 섬 전체 주민 수」 + 2026-09-21 재영님 확정. 동결하지 않으면 **주민을 내보내는 것만으로 지난 주 평균이 오른다**(강퇴 조작). `group_members` 는 (user, group) 한 행이라 세대가 한 벌뿐이므로 표로 동결한다 — **migration V85 `island_weekly_member_counts`**. (당시엔 `left_at` 조차 없어 사후 판정 자체가 불가능했다 — 그 컬럼은 GROMO-2050/V97 가 넣었고, 동결 표는 그 뒤로도 「한 벌뿐인 세대」 때문에 남는다) |
| RK-D01-기준시각 | **동결의 기준은 크론이 «실행된» 순간이 아니라 그 주의 «종료 경계»(다음 주 시작 00:00Z)다.** 가입·이탈 **양방향**을 그 경계로 판정한다 — 시작은 `COALESCE(rejoined_at, created_at) < 경계`, 이탈은 `is_left = false OR left_at >= 경계`(`IslandWeeklyMemberCountRepository.ACTIVE_AT_BOUNDARY_SQL`, **migration V97**). **그래서 배치가 언제 돌든 값이 같다** — 종전의 실행 유예(`ranking.freeze.grace`)는 이탈 시각이 없던 시절의 임시 방편이라 **삭제했다** | 2026-09-21 PR #861 codex P1 → GROMO-2050 이 해소. `created_at` 을 «멤버십 시작»으로 덮지 않은 이유는 그 값이 주민 목록 keyset 정렬의 계약이기 때문(메인 섬 도출·이전은 GROMO-2054 이후 같은 `COALESCE(rejoined_at, created_at)` 축의 최신 순으로 옮겼다 — 시작 시각이 NULL 인 legacy 활성 행은 정렬 맨 아래라 실제 시각이 있는 멤버십보다 앞서지 않는다) |
| RK-D01-결손 | **끝난 주에 동결 행이 없는 섬은 랭킹에서 «빠진다».** 「없으면 지금 인원으로」 라는 대체 경로를 두지 않는다 | 대체 경로가 있으면 동결이 막으려던 조작이 그대로 살아난다. 조용히 틀린 순위보다 비어 있는 순위가 낫다 |
| RK-D02 | **동점은 공동 순위** — 1,1,3(SQL `RANK()`). 다음 순위는 건너뛴다. UUID 오름차순은 같은 점수 안의 «표시 순서»만 고정하는 보조 키이며 순위를 가르지 않는다 | 2026-09-21 재영님 확정 |
| RK-D03 | **끝난 집중만 반영한다** — 진행 중(ACTIVE·PAUSED) 세션은 정렬·표시·`myRank` 어디에도 넣지 않는다 | 2026-09-21 재영님 확정. `lifecycle = COMPLETED` |
| RK-D05 | **섬 귀속 = 세션이 시작할 때 고정한 `focus_session_details.island_id`.** 다른 섬에서 집중한 시간은 이 섬 분자가 아니다 | 2026-09-19 결정 RC-D01 과 같은 축. 기획 정본 「다른 섬에서 집중한 시간은 제외한다」 |
| RK-D06 | 평균 = **정확한 초 합 ÷ 분모를 정수로 내린 값**이고, **그 정수가 곧 정렬 키**다 — 표시값과 순위 기준이 같아야 「화면에는 같은 숫자인데 순위가 다른」 줄이 생기지 않는다. 분모가 없거나 0 이면 그 섬은 참가가 아니다(0점으로 만들지 않는다) | 2026-09-21 구현 결정 |
| RK-D07 | **페이지를 만들지 않는다** — 상위 `limit` 개(기본 30, 최대 100)만 싣고 `nextCursor` 는 언제나 `null` 이다. 목록에서 잘린 사용자는 `myRank` 로 자기 섬 순위를 **전체 모집단** 기준으로 받는다 | 움직이는 집계에 keyset 을 붙이면 페이지 사이에 행이 빠지거나 겹치는데(LLD §4), 페이지가 없으면 그 문제 자체가 없다. 선례 RC-P12-적용(회관 기록 scope=island) |
| RK-D08 | **불변 snapshot 저장소·사용자 역색인·공통 lifecycle 잠금을 만들지 않는다** | 응답이 섬 이름과 평균 초뿐이라 지킬 타인 PII 가 없다. 2026-09-19 RC-P12-적용이 회관 기록에서 내린 같은 판단 |
| RK-D09 | **참가 모집단 = 그 주에 그 섬 귀속 완료 집중이 있었던 섬.** 집중이 0인 섬은 줄 세우지 않고, 그런 섬의 `myRank` 는 `null` 이다 | 0점 참가를 지어내지 않는다(RK-D06 과 같은 결). 모수가 집계 결과라 이어지는 분모·이름 조회도 유한하다 |
| RK-D12 | **해소(GROMO-2050).** 「경계 ~ 배치 실제 실행」 사이의 이탈로 지난 주 분모가 줄던 구멍은 `group_members.left_at`(자진 탈퇴·강퇴·계정 탈퇴 세 경로가 모두 찍는다) + `rejoined_at`(재가입이 멤버십 시작 시각을 갱신한다)로 닫혔다 — 경계 직후 강퇴·탈퇴·재가입 어느 쪽으로도 지난 주 분모가 움직이지 않는다. **남은 것 두 가지**: ① 멤버십 행이 (user, group) 당 하나라 «경계 뒤에 나갔다가 또 돌아온» 한 사람은 그 한 벌이 덮여 경계 시점 소속을 잃는다(닫으려면 멤버십 이력 표가 필요하고, 배치가 경계 직후 한 번만 도는 데 비해 비용이 크다) ② V97 이전에 이탈한 행의 `left_at` 은 NULL 이라 어느 경계에서도 주민이 아닌 쪽으로 판정된다 — 동결은 앞방향 배치라 실제로 손해 보는 주가 없다 | 백필하지 않았다: 관찰 가능한 근거가 없는 값을 지어내지 않는다(GROMO-1995 `loss_reason` 과 같은 판단) |

## 남은 미결

| ID | 미결 | 비고 |
| --- | --- | --- |
| RK-D04 | 지난 주의 **늦은 finish 수용 창**. 지금은 분자를 집중 정본에서 «항상 다시» 계산하므로, 주 경계를 걸친 세션이 늦게 끝나면 지난 주 합이 그만큼 늘어난다(분모는 동결이라 움직이지 않는다). cutoff 를 둘지, 둔다면 언제인지는 미결 | 분모 동결과 달리 분자는 복제하지 않았기 때문에 생기는 창이다. 복제하면 정본과 어긋나므로 지금 구조가 더 안전하다 |
| RK-D10 | **`myRank` 를 공개 계약에 올린 것**은 원본 `/rankings/islands` JSON 에 없던 필드다(`asOf` 와 같은 승인된 추가로 취급). 목록을 상위 N 으로 자르는 대신 자기 순위를 알려 주기 위해 넣었다 — 기획 확인 필요 | RK-D07 과 한 쌍이다. `myRank` 를 뺀다면 페이지가 다시 필요해진다 |
| RK-D11 | **과거 주의 공개 범위**. 지금은 「아직 오지 않은 주」만 422 로 막고 과거는 제한하지 않는다(동결 행이 없는 주는 자연히 빈 결과다). 보존 기간을 둘지는 미결 | |

비적격 `eligibility` enum·섬 썸네일 DTO 는 **주민 랭킹과 함께 사라졌다** — 섬 간 랭킹 응답에 `eligibility` 필드가 없다.
