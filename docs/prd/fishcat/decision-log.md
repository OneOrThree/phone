# Catus 결정 로그

> 제품·정책 결정의 **단일 장부**. 결정은 **여기에 먼저 적고** 해당 도메인 `policy.md` 로 전파한다 — 도메인 문서가 이 표와 어긋나면 이 표가 맞다. 뒤집힌 결정은 지우지 말고 취소선 + 후속 행으로 남긴다([아키텍처 결정 장부](../../architecture/decisions.md) 머리말 규약과 같다).
> ID 는 날짜 안에서 유일하다 — 도메인 문서에서 인용할 때는 「2026-09-18 재영님 결정 D1」 형식으로 날짜와 함께 적는다. 상태: **확정** · **보류** · **확인 중** · **설명 대기** · **폐기**.
> **경계**: 인프라·아키텍처 결정(인증·Kafka/Redis·서비스 경계)은 [`docs/architecture/decisions.md`](../../architecture/decisions.md)(A 번호), 제품·정책 결정(재화·권한·건물·시간축…)은 이 로그. 둘 다 걸치면 이 로그에 적고 A 번호를 가리킨다.
> 숫자만 있는 티켓 번호는 전부 Jira `GROMO-####` 이다. 기획 정본은 `OneOrThree/planning-document` 레포의 `policy-2026-09-14.md`(이하 「기획 정본」)이며 이 레포에는 없다.

## 결정 표

| 날짜 | ID | 결정 | 상태 | 결정자 | 근거/출처 | 반영한 정본 파일 |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-16 | B23 | 기획 v0.6 정책 변경 목록 채택: 섬 물고기 재화 · 건물 각자 몫·총액(1829) · 섬 정원 1~15(1831) · 기록 조회 도서관 이동 · **전망대 주민 랭킹 폐지** · **배 종류 제거** · 단일 로그인 수단. 도메인 LLD 가 각자 고친다 | 확정 | 조재영 | 기획 API v1 0.6-proposed(2026-09-15) 대조. [bff-screens policy](bff-screens/policy.md) B23 | `bff-screens/policy.md` B23. 「전망대 주민 랭킹 폐지」의 확정 내용은 아래 B23·B15 행 |
| 2026-09-16 | B23·B15 | **섬 내 주민 랭킹 엔드포인트 폐지** — `GET /islands/{islandId}/rankings/members`(v0.3 tower 화면 전용)는 기획 v1 98계약에 없고 화면 자체가 B15 로 폐지됐다. BFF 화면에서만 빠지는 것이 아니라 **엔드포인트 자체의 폐기**다. GROMO-1777 은 섬 간 랭킹(`GET /rankings/islands`)만 남는다 | 확정 | 조재영 | `bff-screens/policy.md` B23 「전망대 주민 랭킹 폐지(island-rankings)」 + `bff-screens/planning-v1-mapping.md:63` 「섬 내 주민 랭킹(…)은 기획 v1 98계약에 없다 — 화면 자체가 B15 로 폐지됐으므로 별도 행을 만들지 않는다」. PR #797 claude 판정이 직접 확인(2026-09-18) | **반영 완료(2026-09-21, GROMO-1997)** — `island-rankings/{README,prd,policy,high-level-design,low-level-design}.md` 와 `source-contracts.json` 에서 주민 랭킹을 폐기로 표기했다. LLD §1 에 폐지 근거를, policy RK-P02 에 취소선을 남겼고 `source-contracts.json` 의 `rank` 는 `implementation_status: dropped` 다 |
| 2026-09-16 | B25 | 화면 경로 이름 확정: `boat`→`raft` · `sound`→`playback` · `post-office`→`mailbox`. `raft` 는 기획 화면명 「나의 뗏목」과 **배 종류 폐지**에 맞춘 이름 | 확정 | 조재영 | [bff-screens policy](bff-screens/policy.md) B25. GROMO-1851 「모든 사용자가 같은 기본 뗏목을 쓴다(배 업그레이드 없음)」 | `bff-screens/policy.md` B25 · 배 종류 폐지 반영: `island-appearance/{policy,low-level-design,prd}.md` · `island-shop/{policy,low-level-design,prd}.md` · `realtime-events/low-level-design.md`(2026-09-18 반영) |
| 2026-09-17 | 1909 | 외양 3건: **A01** ~~공동 테마 적용 권한 = 방장만(`SHARED_APPEARANCE`)~~ → **활성 주민 누구나**(2026-09-21 GROMO-2000·D2-개정) · **A03** 선체별 소품 호환 = 모든 선체 공통 · **A05** 기존 캐릭터 자산(HAIR/TOP/BOTTOM/SHOES) 승계 안 함 — 새 모델 분리 | 확정(A01 만 대체) | 조재영 | GROMO-1909. A03 은 배 종류 폐지(B23·B25)로 이후 대상 소멸 | `island-appearance/policy.md` 확정표 · `island-management/permissions.md` 「공동 테마 적용」 행 · `island-shop/policy.md` S01·S06 |
| 2026-09-18 | D1 | **재화 = 개인 지갑 + 섬 통장 «둘 다» 존재. 섬 통장의 재화 = 섬 물고기.** 종전 정본의 「섬 물고기 지갑 신설 금지」(FR-P12)와 「개인 fish / 섬 village_points」 두 통화 이름 중 섬 쪽 이름이 바뀐 것이며 지갑 축은 여전히 둘. 서버 통화 식별자 `village_points` 의 개명은 이 결정에 없다(realtime `EventPayloadValidator` 에 이미 박혀 있음) | 확정 | 조재영 | 2026-09-18 새벽 조정자 세션 답변. **주의**: 기획 정본은 「재화는 물고기 하나이며 섬 소유다. 개인 물고기 지갑은 없다」라고 적고 있어 이 결정과 어긋난다 — 기획 정본 개정은 별도 | `focus-rest-session/policy.md` FR-P12 · `island-construction/policy.md` C05 · `island-quests/policy.md` Q05 · `island-shop/{policy,low-level-design,prd}.md` · `realtime-events/low-level-design.md` §2 #7 |
| 2026-09-18 | D2 | ~~**섬 물고기 지출 권한(건설 실행·공동 구매) = 섬 설정, 방장이 토글.** 방장 허용 / 주민 「설정 가능」. 판정 이름 `SHARED_PURCHASE` 유지. 그룹 단위 enum 선례 `Group.invitePermission`/`GroupPermissionScope`(data-api 실재 확인) 재사용 방향. 토글 기본값·설정 API 는 이 결정에 없다~~ **2026-09-21 D2-개정으로 대체됨** — 기획 정본이 건설=방장 / 공동 구매·외양=주민 누구나로 축을 고정해 토글 자체가 사라졌다 | 대체(D2-개정) | 조재영 | 2026-09-18 새벽 조정자 세션 답변 | `island-management/permissions.md` 「건설 실행」·「공동 상품 구매」 행 · `island-construction/policy.md` C08·C13·P-D01 · `island-shop/policy.md` S02 · `island-appearance/policy.md` 권한 문단 · `focus-rest-session/policy.md` 권한 문단 |
| 2026-09-21 | D2-개정 | **섬 물고기 지출 권한을 행동별로 갈라 섬 설정 토글을 폐기한다.** ① 건설(목표 선택 PUT·건설하기 POST) = **방장만** — 게스트 방장도 가능(건설은 게스트 축을 타지 않는다). ② 공동 구매(상점 상품·축음기 음원) = **활성 주민 누구나, 게스트 제외**. ③ 공동 외양(섬·건물 테마) = **활성 주민 누구나**(게스트 축 없음 — 보유한 것을 적용할 뿐). 판정 이름 `SHARED_PURCHASE` 는 유지하되 축이 둘로 갈렸다. `groups.shared_purchase_permission` 은 읽는 곳이 없고 열 제거는 별도 마이그레이션 | 확정 | 조재영 | 기획 정본 `policy-2026-09-14.md` 「방장이 회관의 다음 건물 선택에서 목표를 정하면」·「방장이 건설하기를 누를 때 … 차감하고 공사를 시작한다」 대 「주민 누구나 섬 물고기로 상점 상품·축음기 음원을 구매할 수 있다」·「공동 섬·건물 테마도 주민 누구나 적용·해제한다」·「상점 구매를 처음 시도할 때 소셜 로그인을 요청한다」. 구현 GROMO-2000 — #875(GROMO-1999)에 함께 실림 | `island-construction/policy.md` C13 · `island-shop/policy.md` S02 · `island-shop/low-level-design.md` §4 · `island-management/permissions.md` 「건설 실행」·「공동 상품 구매」 행 · `island-appearance/policy.md` 권한 문단 · `focus-rest-session/policy.md` 권한 문단 · `construction/support/SharedPurchase.java` |
| 2026-09-18 | D3 | **퀘스트 작성·수정 = 방장만.** 기존 챌린지 `GroupMemberRole.OWNER` 검사 선례 그대로. claim 은 원본대로 주민 요청·서버 판정 | 확정 | 조재영 | 2026-09-18 새벽 조정자 세션 답변 | `island-management/permissions.md` 「퀘스트 작성/수정」 행 · `island-quests/policy.md` QQ04 |
| 2026-09-18 | D4 | **건물 집합 = 기획 7개 선형(1829 표)**: 회관 60마리·1분 → 게시판 240·15분 → 축음기 1,360·30분 → 도서관 2,720·1시간 → 우체통 4,080·1시간 30분 → 전망대 5,440·2시간 30분 → 상점 6,800·4시간(총액·공사 시간). 회관·게시판은 「섬 통장 합산」, 축음기 이후는 「각자 몫 n빵」. 종전 선택 분기·「전망대 AND 우체통 → 상점」 폐지. **도서관 기능은 GROMO-1822 미확정**(건물은 있되 기능 미정). 기획명 축음기 = 서버명 방송기/`gram` — 코드 식별자(`gram`·`playback`)가 걸려 있어 서버명을 바꾸지 않는다 | 확정 | 조재영(값은 기획 GROMO-1829, 2026-09-15) | GROMO-1829 코멘트 #11077(안수빈, 2026-09-15): 「섬 통장 합산: 섬 물고기가 총액만큼 모이면 건설」 · 「각자 몫 n빵: 주민마다 총액 ÷ 인원만큼 채우면 건설 (사람이 많을수록 빨리 지음)」. 기획 정본: 「축음기부터는 대상 주민이 총액 ÷ 대상 인원(올림)을 각자 채우고, 방장이 건설하기를 누를 때 총액을 섬 잔액에서 차감한다」 · 「각자 몫은 목표를 고른 뒤부터 모은 물고기로 판단한다」 | `island-construction/{policy,high-level-design,prd,low-level-design}.md` · `api-platform/policy.md` 대조표 |
| 2026-09-18 | D5 | **집중 보상 산식 확정 승격**(1830): 유효 집중 60초당 1마리 · 휴식 제외 · 주민·섬별 하루 480마리 상한(상한에 닿아도 집중 기록은 쌓임) · 퀘스트 개인 달성 +10마리 · 전원 달성 보너스 대상 주민 수 × 5마리 · 종류별 차등 없음. ~~보상의 개인 지갑/섬 통장 귀속(비율)은 확인 중~~ → ~~D5-귀속 행에서 개인 50% + 섬 통장 50% 로 확정~~ → **2026-09-19 D5-귀속-개정으로 섬 통장 100%** — 재영님 「난배 비율 정해져 있지 않냐」 → 근거 조사 결과 아래 | **산식 확정 / 귀속은 D5-귀속 행에서 확정(이 행의 「확인 중」은 그 전 상태)** | 조재영(값은 기획 GROMO-1830, 2026-09-15) | GROMO-1830 코멘트 #11072(안수빈). **귀속 근거 조사(2026-09-18, 판정 c = 비율 근거 없음)**: ① 1829·1830 코멘트 전체에 개인/섬 분배 비율 없음 — 1830 의 「주민 1명이 섬 1곳에서 집중으로 낚는 물고기는 최대 480마리」는 상한의 단위이지 분배가 아님. ② 1829 의 「각자 몫 n빵」은 **건설 문턱**(주민별 누적 획득 기록으로 판정, 차감은 섬 잔액에서 총액)이지 보상 분배가 아님 — 기획 정본 「물고기 잔액과 주민별 누적 획득 기록은 구분한다」. ③ 유일한 서면 규칙은 기획 정본 「재화는 물고기 하나이며 섬 소유다. 개인 물고기 지갑은 없다」·「집중한 섬에만 물고기와 집중 실적이 쌓인다. 복수 소속 섬에 중복 적립하지 않는다」 = **전부 섬**인데, 그 전제(개인 지갑 없음)를 D1 이 뒤집었으므로 비율로 쓸 수 없음. ④ `bff-screens/policy.md` B23~B25·`docs/prd/fishcat/**` 에 비율 없음(종전 설계의 `allocation{personalFishAdded,constructionFishAdded}`·E=P+C 를 FR-D02 모델과 동일시해 D6 로 폐기했다고 적었으나 — **이 결론은 2026-09-18 아침 D5-귀속으로 뒤집혔다**: D6 가 폐기한 것은 «세션 중 완공 시 시간 분할·초과 환류»이지 개인/섬 배분 축이 아니어서, 두 형태 모두 되살아났다). GROMO-1804(배분 확정 BE 티켓)는 코멘트 0건·미착수 | `focus-rest-session/policy.md` FR-D01(산식 확정·귀속 칸 보류) · `api-platform/policy.md` 대조표 · `island-quests/policy.md` Q05·QQ05 |
| 2026-09-18 | D6 | **FR-D02 폐기 — 「쌓이면 건설한다」.** 집중 보상은 지갑에 쌓이고 건설은 쌓인 물고기로 실행한다. 세션 도중 완공 시 시간별 분할·종료 시점 배분·초과분 개인 환류라는 개념 자체가 없다 | 폐기(FR-D02) | 조재영 | 2026-09-18 새벽 조정자 세션 답변. `doc/decisions/` 는 개인 폴더라 손대지 않았다 | `focus-rest-session/policy.md` FR-D02·본문 · `focus-rest-session/low-level-design.md` 정산 문단 · `island-construction/policy.md` C09·P-D02 · `island-construction/{high-level-design,prd,low-level-design}.md` |
| 2026-09-18 | D7 | **RC-D02(복수 기기 표시·집계 병합) = 보류, 가장 나중에.** 저장 축(RC-P07·08, 기기별 분리)만 확정. 표시·집계는 출시 후 결정 | 보류 | 조재영 | 2026-09-18 새벽 조정자 세션 답변 | `island-records/policy.md` RC-D02 |
| 2026-09-18 | D8 | **모든 시간 UTC · 하루 리셋 UTC 00:00 · 글로벌 출시 전제.** `docs/conventions/date-axis.md` §2 「서버 축 = KST 고정」과 정면 충돌. 코드·규약은 지금 고치지 않는다 — 영향 조사 뒤 별도 티켓(1930 예정)에서 단계 계획 | 확정(전환 예정) | 조재영 | 2026-09-18 새벽 조정자 세션 답변. 1830 의 「하루 기준 시간대는 미정인 초기화 시간대 항목을 따름」이 이 결정으로 닫힌다. **영향 조사(2026-09-18 완료)**: 영향 ≈ 272 파일(서버 main 43 · 서버 테스트 81 · 앱 86 · 문서 60). `ZonePolicy.KST` 호출자는 7파일뿐이고 자체 `ZoneId` 상수 24클래스 + 크론 `zone="Asia/Seoul"` 24개가 따로 있어 상수 하나로 안 바뀐다. **재계산 불가 데이터**: `daily_screen_time_stats.date`·`group_challenge_members.usage_date`·`league_rank_snapshots`·`goal_effective_from`(원본 instant 없음). 가장 위험: ① 주간 리그 정산 이중/누락(`league_weekly_results` 유니크 키가 KST 월요일 instant) ② 날짜 문자열 멱등키(`focusGoal:…:{date}`·`stGoal`·`streak:`)로 재화 이중 지급·푸시 이중 발송 ③ 스크린타임 앱 로컬 정오 앵커와 서버 환산이 쌍이라 서버만 바꾸면 깨지는 지역이 조용히 바뀜. 별도 티켓(1930 예정)에서 단계 계획 | `docs/conventions/date-axis.md` 머리 단락만. 그 밖의 KST 규약(`api-platform/policy.md` P15·KST 입력 5종, `focus-rest-session/policy.md` 날짜 입력, `island-quests/policy.md` Q02, `island-records/policy.md` RC-P02, `island-rankings/policy.md` RK-P01 「주 = KST 월요일 00:00 ~ 다음 월요일 00:00」)은 **현행 유지** — 1930 에서 개정. RK-P01 의 주 경계는 D8 로 재정의되며 `league_weekly_results` 유니크 키 위험(위 ①)과 같은 축이라 1777·1930 이 함께 본다 |
| 2026-09-18 | D9 | **공지 작성·수정·삭제 = 현행 그대로.** 방장 무조건 + 방장이 개별 허용한 주민(`announcement_permission=ALLOW`). 수정·삭제도 작성자 검사 없음(타인 공지도 대상) | 확정 | 조재영 | 2026-09-18 새벽 조정자 세션 답변. main `GroupAnnouncementService`/`GroupMember` 의 legacy 동작([island-board policy](island-board/policy.md) B02) | `island-management/permissions.md` 「공지 작성/수정/삭제」 행 · `island-board/policy.md` BQ01·B03 |
| 2026-09-18 | D10 | **초대 코드 = 무기한 · 1인 1코드 재사용 · 승인제 유지**(A3-a + A5-a). 코드 형식은 기존 slug 알파벳(31자)·길이 8자 재사용. 「폐기」 상태 축은 data-api 에 없다 — outbox `link.revoked` 사건뿐 | 확정 | 조재영 | 2026-09-18 새벽 조정자 세션 답변. 근거 결정표는 GROMO-1805 브랜치의 `open-decisions.md` §1(아직 main 에 없음 — 이 로그는 가리키기만 한다) | `island-membership/prd.md` IM-D01~D03 |
| 2026-09-18 | D11 | **기존 코인 잔액 이관 안 함.** 1909 A05(보유품 승계 안 함)와 같은 결 | 확정 | 조재영 | 2026-09-18 새벽 조정자 세션 답변 | `island-shop/policy.md` S01 · `island-shop/prd.md` · `api-platform/policy.md` 미결 목록 |
| 2026-09-18 | FR-D03 | **강퇴 시 진행 세션 = B1 「강제 종료, 미정산」.** 강퇴 TX 가 같은 잠금 아래 진행 세션을 서버 시각으로 원자 종료하고 그 시점까지의 물고기는 정산하지 않는다. ~~B3(종결하되 개인 확정 보상 보존)~~ 추천은 채택되지 않았다 — 재영님 「강제 종료가 구현 깔끔」. 자진 탈퇴는 그대로 「먼저 끝내고 나가라」 | 확정 | 조재영 | 2026-09-18 아침 조정자 세션 (AskUserQuestion 응답). 선택지·경로 매핑은 PR #795 `island-membership/open-decisions.md` §2 | `focus-rest-session/policy.md` FR-D03 · `island-management/low-level-design.md` §5 강퇴 행 · `island-management/high-level-design.md` · 구현 GROMO-1924 |
| 2026-09-18 | FL-결정-1 | **게스트도 편지를 보낼 수 있다.** `POST /letters` 에 게스트 차단 분기를 두지 않는다 | 확정 | 조재영 | 2026-09-18 아침 조정자 세션 (AskUserQuestion 응답). 1867 에는 없던 항목 | `friend-letter/low-level-design.md` §4 결정-1 |
| 2026-09-18 | FL-결정-2 | **발송은 게이트 없음, 열람은 게이트 있음.** 받는 친구의 섬에 우체통이 없어도 보낼 수 있고, 받은 편지는 **소속 섬 중 우체통이 완공된 곳이 있어야** 확인할 수 있다 | 확정 | 조재영 | GROMO-1867 코멘트 #11088(안수빈 2026-09-16) 값을 재영님이 「1867 티켓 내용 가져와서 채워보기」로 채택 | `friend-letter/low-level-design.md` §4 결정-2 |
| 2026-09-18 | FL-본문 | **편지 본문 상한 500자.** HLD §2.1 의 1000 은 「구현 착수 전 기획 확인」이 걸려 있던 값이라 ~~1000~~ → 500 으로 확정. `letters.content varchar(500)` | 확정 | 조재영 | 2026-09-18 아침 조정자 세션 (AskUserQuestion 응답) | `friend-letter/{high,low}-level-design.md` |
| 2026-09-18 | D5-귀속 | ~~**집중 보상 귀속 = 개인 지갑 50% + 섬 통장 50%.** 비율 자체는 운영값이라 설정으로 조정한다(초기값 50/50).~~ **2026-09-19 D5-귀속-개정으로 대체됨.** 이로써 `E=P+C` 보존식과 `allocation{personalFishAdded,constructionFishAdded}` 가 **되살아난다**(이 부분은 개정 후에도 유지 — P=0) — D6 가 폐기한 것은 «세션 도중 완공 시 시간 분할·초과 환류»이지 개인/섬 배분 축이 아니었다 | 대체(D5-귀속-개정) | 조재영 | 2026-09-18 아침 조정자 세션 (AskUserQuestion 응답). 앞선 확인에서 1829(「섬 통장 합산 / 각자 몫 n빵」은 건설 문턱이지 분배가 아님)·1830(480 은 상한 단위)·기획 정본 어디에도 비율이 없다는 것이 드러났고, 그 자리에서 반반으로 정했다 | `focus-rest-session/policy.md` FR-D01 · `low-level-design.md`(E=P+C·allocation) · `prd.md` |
| 2026-09-19 | D5-귀속-개정 | **집중 보상을 포함한 모든 재화는 섬 통장 100% 귀속.** 개인 지갑(`user_fish_wallets`)은 테이블만 두고, 개인 돈벌기 방향 논의 후 개발. D5-귀속(50/50)을 대체한다. 산식(60초당 1마리 · 하루 480 상한)과 정책 revision 표는 그대로이고 비율만 `personal_share_percent = 0` 이다 | 확정 | 조재영 | 2026-09-19 조정자 세션 전달(재영님 결정). D1(개인 지갑 존재)은 뒤집지 않는다 — 지갑은 남고 집중 보상이 거기로 가지 않을 뿐이다 | `focus-rest-session/{policy,low-level-design,prd}.md` · `api-platform/policy.md` · 구현 GROMO-1924(V67 시드 revision 1 = 60·480·0) |
| 2026-09-18 | D1-재확인 | **개인 지갑 + 섬 통장 「둘 다」 유지.** 기획 정본 「개인 물고기 지갑은 없다」와의 충돌은 **기획 정본을 개정하는 쪽**으로 해소한다 — 이 로그가 우선이다 | 확정 | 조재영 | 2026-09-18 아침 조정자 세션 (AskUserQuestion 응답). 「섬 통장 하나로 회귀」 선택지를 보고 둘 다 유지를 골랐다 | 기획 정본 `policy-2026-09-14.md` 와 GROMO-1849 문구 개정은 **기획 몫**(이 레포 밖) |
| 2026-09-18 | FL-형태 | ~~**편지는 일반 우편함 — 삭제하지 않는다.**~~ **2026-09-21 FL-형태-개정으로 대체됨.** | 대체(FL-형태-개정) | 조재영 | 2026-09-18 아침 조정자 세션 (AskUserQuestion 응답). ⚠️ **GROMO-1867 #11088 기획 결정과 정반대** — 기획 정정이 필요하다(재영님이 #11117 에서 「보낸 목록이 필요한가, 보내고 날리는 게 낫지 않나」로 이미 의문을 달아 둔 항목) | `friend-letter/{high,low}-level-design.md` — 보관 모델 |
| 2026-09-18 | FL-결정-3 | ~~친구 삭제 후 편지 보존 — 대기~~ **2026-09-21 확정: 삭제(B), 「아직 확인하지 않은」 편지에 한한다.** 친구를 끊으면 두 사람 사이의 미확인 편지를 양방향으로 소프트 삭제한다. 이미 읽은 편지는 건드리지 않는다 — 그건 수신자가 닫아서 지운다(FL-형태-개정) | 확정 | 조재영 | `policy-2026-09-14.md` 「친구를 삭제하면 서로 편지를 보낼 수 없고 아직 확인하지 않은 편지도 지운다」. FL-형태-개정이 「일반 우편함」을 거두면서 충돌이 해소됐다 | `friend-letter/low-level-design.md` §결정-3 · 구현 GROMO-2002(`FriendService.deleteFriend` → `LetterRepository.softDeleteUnreadBetween`) |
| 2026-09-21 | FL-형태-개정 | **편지는 기록으로 남지 않는다.** 받는 사람이 열었다가 «닫으면» 지워지고, **보낸 사람 목록에서도 사라진다**. 닫기는 별도 명령(`DELETE /letters/{id}`, 수신자만) — 상세 조회(GET)에 삭제를 얹지 않는다. 그래야 「열었지만 아직 안 닫은」 상태가 표현된다. FL-형태(일반 우편함)를 대체하고, 결과적으로 GROMO-1867 #11088 기획과 다시 일치한다 | 확정 | 조재영 | `policy-2026-09-14.md` 「친구 편지는 기록으로 남기지 않는다…」. 재영님이 #11117 에서 이미 달아 둔 의문(「보낸 목록이 필요한가」)의 귀결 | `friend-letter/low-level-design.md` §1.16 · 구현 GROMO-2002 |
| 2026-09-21 | FL-누출-수용 | **발신자가 「사라진 시점」으로 상대의 열람을 알게 되는 누출을 «알고» 수용한다.** 닫기가 양쪽에서 편지를 지우므로, 보낸함 `isRead` 를 항상 false 로 고정해 열람 여부를 숨기는 계약(LLD §1.13)과 형식상 모순이다. 숨기려면 삭제를 발신자·수신자 2컬럼으로 나눠야 하는데(LLD §2.1 이 명시적으로 배제한 모델) 그 비용을 치르지 않는다 — **버그로 보고 갈아엎기 전에 이 결정부터 뒤집을 것** | 확정 | 조재영 | 2026-09-21. 정책이 「보낸 사람 목록에서도 사라진다」로 명시한 것의 불가피한 대가 | `friend-letter/low-level-design.md` §1.16 경고 상자 · `Letter` 엔티티 주석 · `InternalLetterService.close` javadoc · `InternalLetterIntegrationTest` |
| 2026-09-16 | 1867-친구검색 | 친구 검색은 **닉네임 정확 일치**(대소문자 무시). 닉네임은 중복 불가(대소문자 무시), 만들거나 바꿀 때 앞뒤 공백 없이 저장 | **확정 · 반영됨(2026-09-21)** | 안수빈 → 조재영 | GROMO-1867 #11088 → `policy-2026-09-14.md` 「친구 관리·우체통 대화」가 같은 문구로 채택. 재영님 의문 #11117 「pg_trgm 이 알아서 해 줄 것 같다」는 채택되지 않았다 — 부분 일치는 「닉네임 두 글자로 사용자 훑기」를 허용한다 | `friend-letter/low-level-design.md` §1.17 · 구현 GROMO-1996(V89 `uq_users_nickname_lower` · `/friends/search` 신설 · 비친구 tier·occupation 가림) |
| 2026-09-16 | 1867-요청 | 거절된 친구 요청은 **바로 재신청 가능**. ~~**차단은 2.0.0 에 넣지 않는다**(신고센터 1673 와 별개)~~ **2026-09-22 「차단-도입」과 RP-차단 행으로 대체됨** — 2.0에 차단을 포함하고 최종 효과는 RP-차단을 따른다 | 기획 확정 · 승인 대기 | 안수빈 | GROMO-1867 #11088 | 서버 정본 미반영 — GROMO-1894 의 친구 7종은 재신청을 막지 않아 이 결정과 이미 맞는다 |
| 2026-09-22 | 차단-도입 | **사용자 차단은 2.0.0 에 넣는다** — 1867-요청의 「차단은 2.0.0 에 넣지 않는다」를 대체한다. App Review Guideline 1.2 와 Google Play UGC 정책이 1:1 상호작용(친구 편지)에 차단을 요구한다. 기존 `user_blocks(blocker→blocked)` 를 재사용하고 마이그레이션은 없다. POST·DELETE 는 멱등, GROMO-1975 구현 범위는 친구 목록·검색·받은 편지함의 **표시 필터 1단계**이며 친구 관계·편지 원문을 건드리지 않는다. 2.0 최종 차단 효과는 2026-09-22 RP-차단과 2026-09-24 RP-차단-정합이 정한다. 공개 표면은 `GET·POST /blocks` 와 `DELETE /blocks/{blockedUserId}` | 확정 | 조재영 | GROMO-1975 | `friend-letter/low-level-design.md` §1.18 · `contracts/business-satellite-api.yaml` · 구현 `InternalUserBlockController`·`UserBlockService` |
| 2026-09-16 | 1867-메인섬 | **메인 섬**(신규 개념): 모든 사용자 1개, 첫 섬이 자동. 내 뗏목의 내 정보에서 변경. 메인 섬에서 탈퇴·강퇴되면 가장 최근 가입 섬으로 자동 변경 + 알림. 표시는 친구 목록·프로필에만이고 물고기 적립·랭킹에는 영향 없음. **마지막 섬은 탈퇴 불가**. (「앱 시작 섬 = 마지막 접속 섬」은 1867 이 같이 적었지만 **메인 섬의 정의가 아니다** — 그건 `current_island_id` 가 이미 하는 일이다. 두 축을 한 문장에 섞지 말 것) | 기획 확정 · 승인 대기 | 안수빈 | GROMO-1867 #11088. 재영님 의문 #11117 「마지막 섬 탈퇴 불가는 애매하다」 | 서버 정본 미반영. ⚠️ **현재 섬 컨텍스트(`user_island_contexts.current_island_id`, GROMO-1759)와 별개 속성**이다 — 같은 것으로 합치지 말 것 |
| 2026-09-19 | SH-테이블 | **상점은 테이블만 선구축, 가격은 추후 확정.** GROMO-1781 은 스키마(Flyway V74 + JPA 엔티티·repository + schema.dbml)만 만든다 — ~~공개·내부 엔드포인트와 구매 로직은 없다~~ → **같은 날 N24 로 개정: 엔드포인트·구매 로직까지 구현, 시드·가격만 없다**. 가격 행·시드·자리표시 가격을 넣지 않고, 「승인 가격 없음」은 `price` NULL(= 구매 불가)로만 표현한다 — 0원 금지(CHECK). 가격(S03)·기본 3곡/초기 자산(S04)은 여전히 미결 | 확정 | 조재영 | 2026-09-19 조정자 세션 답변 | `island-shop/policy.md` S03·S04 · `server/data-api/.../V74__island_shop.sql` · `server/data-api/docs/db/schema.dbml` |
| 2026-09-19 | SH-BG18 | **구매 내역은 섬 귀속.** 주문은 섬 하나에 속한다(`shop_orders.island_id` NOT NULL) — `scope=personal` 도 경로 섬 안의 본인 주문만 본다. 섬 무관 전체 구매 이력(기획 `my-orders`)은 만들지 않는다. [bff-screens policy](bff-screens/policy.md) BG18 해소 | 확정 | 조재영 | 2026-09-19 조정자 세션 답변. 종전 LLD §1 「섬에서 무엇을 샀든 본인 개인 주문」과 충돌 → LLD 를 고쳤다 | `island-shop/low-level-design.md` §1 · `bff-screens/policy.md` BG18 · V74 `shop_orders` |
| 2026-09-19 | SH-재화 | **모든 재화는 섬 귀속 — 상점은 섬 통장(`island_wallets`, `village_points`)만 차감한다.** 개인 물고기 지갑(`user_fish_wallets`, PR #815)은 나중을 위한 테이블로만 존재하고 상점은 차감하지 않는다. 주문 테이블은 `payer_type`(지금 `island` 만)·`payer_user_id`(지금 NULL)를 두어 개인 결제자를 나중에 CHECK 확장만으로 붙일 수 있게 한다 — 구현은 안 한다. D1-재확인(지갑 둘 다 유지)은 그대로이며 이 결정은 **상점의 결제 축**을 섬으로 좁힌다(집중 보상 쪽 D5-귀속-개정과 같은 결) | 확정 | 조재영 | 2026-09-19 조정자 세션 답변 | `island-shop/policy.md` 경제 소유 행렬 주석 · V74 `shop_product_revisions.currency`·`shop_orders.payer_*` CHECK |
| 2026-09-19 | N24 | **상점은 「데이터만 넣으면 동작하게」 만든다 — SH-테이블 개정.** GROMO-1781 이 지갑·상품 목록(`category=personal\|island\|sound`)·상품 상세·구매(`POST …/shop/orders`)·섬 귀속 내역(`GET …/shop/orders`, SH-BG18) 다섯 계약의 Data·Business 구현을 끝낸다. 시드·운영 가격은 **여전히 넣지 않는다**(S03·S04 미결 그대로) — 카탈로그 자산·판매 revision·발행본·활성 포인터 행을 운영이 넣는 순간 판매가 열린다. 가격 NULL 은 `available=false` + 구매 409 `STATE_CONFLICT`(0원 판매 금지). 결제는 SH-재화대로 섬 통장만(개인 상품도), 지출 권한은 `SHARED_PURCHASE`(D2, 기본 `OWNER_ONLY`), 판매 revision 의 `required_building`·`required_product_id` 선행을 구매 TX 가 강제한다 | 확정 | 조재영 | 2026-09-19 조정자 세션 답변 | `island-shop/policy.md` S03 · `server/data-api/.../shop/service/ShopService.java` · `server/business-api/.../usecase/ShopUseCase.java` |
| 2026-09-19 | N25 | **활성 카탈로그가 없으면 빈 목록이다 — 오류가 아니다.** 활성 발행본 포인터 행이 없으면 상품 목록은 `items:[]`(화면 `shop`·`playback` 의 `products` 조각도 200), 상품 상세·구매는 404 `PRODUCT_NOT_FOUND` 다 | 확정 | 조재영 | 2026-09-19 조정자 세션 답변 | `island-shop/low-level-design.md` §2.2 · `ShopService#products` |
| 2026-09-19 | Q-1·Q-2 | **퀘스트 보상 귀속 = 섬 통장 100%.** 개인 지갑 지급 없음. 정산은 회차당 섬 단위 1회(도메인 유일 (islandId, occurrenceId, kind)). 금액은 D5 값 그대로 «달성 주민 × 10 + (전원 달성이면) 대상 주민 수 × 5» 를 섬 통장에 적립한다. 수령(claim)은 대상 전원 달성일 때만 가능하므로(미달성 claim 은 409 `STATE_CONFLICT`, 조회의 `claimBlockedReason=MEMBERS_INCOMPLETE`) 달성자 = 대상 수이고, 실제 적립량은 **대상 주민 수 × 15** 다. 부분 달성 회차는 적립 0 | 확정 | 조재영 | 2026-09-19 조정자 세션(GROMO-1773 착수 지시). Q05(개인 +10 · 보너스 × 5)와 Q06(같은 회차 정산 1회)을 함께 만족시키는 읽기 — 개인 +10 을 따로 줄 수 있는 두 번째 정산이 Q06 으로 없으므로 «달성자 × 10» 은 전원 달성 때 대상 × 10 으로 접힌다. D5-귀속(집중 보상 50/50)과 별개 축이다 | `island-quests/policy.md` Q05·Q06·QQ05 · `island-quests/low-level-design.md` §2 claim · 구현 GROMO-1773(`IslandWalletTransactionType.QUEST_SETTLEMENT`) |
| 2026-09-21 | Q-1-개정 | **개인 수령과 전원 자동 보너스를 분리한다.** 개인 달성 +10 은 «달성한 주민이 받기를 눌러» 각자 받고, 전원 달성 보너스(대상 주민 수 × 5)는 «수령 없이 즉시» 1회 적립된다. 적립처는 둘 다 섬 통장으로 Q-1 그대로이고, 바뀐 것은 «수령의 단위»다 — 종전 Q-1 의 「전원 달성 때만 수령, 대상 × 15 한 번」을 대체한다. 같은 회차에서 각 주민의 달성 보상과 전원 보너스는 각각 한 번만 지급한다. 5명 전원 달성·전원 수령이면 개별 50 + 보너스 25 = 75 | 확정 | 기획 정본 | 기획 정본 `policy-2026-09-14.md` 「일일 퀘스트와 보상」 절(planning-document 레포)이 처음부터 이렇게 적고 있었다 — Q-1 은 그 절을 Q06(회차 1회 정산)과 맞추려다 개인 수령을 접어 버린 읽기였다. 구현 GROMO-1991 | `island-quests/policy.md` Q04·Q05·Q06 · `island-quests/low-level-design.md` §2 claim·§5 · `server/data-api/.../V83__island_quest_personal_claims.sql` · 앱 GROMO-2014 |
| 2026-09-19 | Q-3 | **판정 대상(cohort) = 회차 시작 때 고정**(QQ01). 이후 가입자는 제외. 탈퇴·강퇴·계정 탈퇴한 주민은 cohort(분모)에서 빠진다. 측정 불가 주민도 분모에서 뺀다. 분모가 0 이면 전원 달성이 아니다(0명 전원 성공 금지 유지) | 확정 | 조재영 | 2026-09-19 조정자 세션 | `island-quests/policy.md` QQ01 · `island-quests/low-level-design.md` §3·§4 |
| 2026-09-19 | Q-4 | **회차 = UTC 하루(00:00Z 경계), 매일 반복**(QQ02). 정의 PATCH 는 진행 중 회차를 바꾸지 않고 **다음 회차부터** 적용한다. 자정을 넘는 focus 창은 회차가 하루라서 받지 않는다(422) | 확정 | 조재영 | 2026-09-19 조정자 세션. 자정 넘는 창 거절은 «UTC 하루 회차»에서 따라 나온 구현 판단 | `island-quests/policy.md` QQ02 · `island-quests/low-level-design.md` §2 quest-edit |
| 2026-09-19 | Q-5 | **측정 규칙**(QQ03): focus 는 초 단위 정확 비교(목표분 × 60, 관용치 없음) — 그 섬에 귀속된 세션의 ACTIVE 구간만, REST 제외, 회차 창(UTC)에 잘라 합산. screen 은 다음 날 **12:00 UTC 까지** 보고 유예 — 유예 뒤에도 판정할 값이 없는 주민은 측정 불가(분모 제외). 복수 기기 병합은 ticket 1806 미결이라 **기존 (주민, 날짜) 단일 값**을 쓴다 | 확정(복수 기기 1806 대기) | 조재영 | 2026-09-19 조정자 세션. 퀘스트 조회·수령 마감(focus 다음 날 12:00 UTC · screen 다음 날 24:00 UTC)은 이 유예에서 따라 나온 구현 판단 — GROMO-1773 PR 에 기록 | `island-quests/policy.md` Q03·QQ03 · `island-quests/low-level-design.md` §4 |
| 2026-09-19 | Q-6 | **섬 퀘스트의 날짜 축은 처음부터 UTC.** D8(전 서비스 UTC, 전환은 1930)을 퀘스트는 신규 기능이라 전환 없이 바로 적용한다. Q02 의 «Asia/Seoul» 문구를 덮는다. 표현은 `date-axis.md` §7.5 권고의 버킷 축(UTC 고정)을 따르되, 신규 계약이라 구앱 호환이 필요 없으므로 `timezone` 은 생략 또는 `"UTC"` 만 받는다(`Asia/Seoul` 은 창 시각을 다른 축으로 읽게 만들어 400) | 확정 | 조재영 | 2026-09-19 조정자 세션. ⚠️ `daily_screen_time_stats.date` 는 KST 라벨(`ScreenTimeService` 가 보고 시각을 KST 로 환산, §7.2 재계산 불가)이라 UTC 회차로 읽으면 측정 창이 9시간 밀린다. 그래서 **스크린 퀘스트는 스크린타임 날짜 축 UTC 전환(1930) 전까지 생성 불가**(생성·수정 422 `OUT_OF_RANGE` field=type — 2026-09-19 조정자 결정, GROMO-1773 로컬 리뷰 [필수] 반영) | `island-quests/policy.md` Q02 · `island-quests/low-level-design.md` §1·§2·§6 · `docs/conventions/date-axis.md` §7.5 는 수정 없음(가리키기만) |
| 2026-09-19 | V-읽기 | **다른 섬 방문자의 읽기 범위 = GROMO-1904 안.** 방문자 = 그 섬의 현재 활성 주민이 아닌 사용자(가입 신청 대기자 포함). 방문자가 **읽을 수 있는 것**: 마을회관 섬 정보 등록증(이름·소개·주민 수/정원) + 주민 목록(닉네임·고양이 외형·방장 여부), 게시판 공지·댓글·퀘스트(주민별 달성률 포함) — 전부 읽기 전용. **주민 전용**: 모든 쓰기(공지·댓글·퀘스트), 가입 신청 관리, 주민 관리(강퇴·위임), 초대 링크, 섬 정보 수정, 공동 가계부, 목각 건물·청사진, 주민 프로필·개인 기록, 나머지 건물 5개(축음기·도서관·우체통·전망대·상점). 도서관도 주민 전용이라 BG11 잔여는 「열지 않음」으로 닫는다. 2.0 에는 공개/비공개 설정이 없어 모든 섬을 공개로 본다 | 확정 | 안수빈(기획) · 조재영(승인) | GROMO-1904 댓글 11122·11123·11126(2026-09-19). 구현 GROMO-1937(백엔드)·1936(앱) | `island-management/permissions.md` 「섬 주민 목록」 · `island-board/policy.md` B01 · `island-membership/low-level-design.md` 방문자 DTO · `island-quests/low-level-design.md` §1 · `bff-screens/{low-level-design,policy}.md` §11·BG11 |
| 2026-09-19 | R-1 | **Data → realtime 사건 전송 = HTTP·Kafka 이중(알림 서버와 같은 구조).** Data outbox relay 가 `REALTIME` 을 HTTP(realtime `POST /internal/events`, 전용 토큰 `SVC_TOKEN_DATA_TO_REALTIME` — #819)와 Kafka(`realtime-events` 토픽) 둘 다 보낼 수 있고, realtime 은 두 입구를 **한 처리기**로 받아 `eventId` 로 중복을 거른다. **기본 HTTP**, Kafka 는 플래그(기본 꺼짐 — Data `OUTBOX_RELAY_REALTIME_KAFKA_ENABLED`, realtime `REALTIME_EVENTS_KAFKA_ENABLED`). 다중 인스턴스는 인스턴스마다 모든 사건을 받아야 한다(Kafka 인스턴스별 소비 그룹 · HTTP 는 Redis 재분배) — 지금은 단일 인스턴스라 구현하지 않는다 | 확정 | 조재영 | GROMO-1954. 선례: `server/notification` README 「Kafka·HTTP 공통 처리」 | `docs/conventions/outbox-events.md` §2.4·§2.8 · `OutboxRelayConfig` · realtime `InboundEventService`·`KafkaEventInbound` |
| 2026-09-19 | 계정-Q03 | **`catColor` 카탈로그 = 앱 2.0 의 6종 `black`·`ginger`·`cream`·`gray`·`white`·`calico`**(표시명 검정·치즈·크림·회색·흰색·삼색). 서버는 이 ID 만 저장한다(Business 422 `OUT_OF_RANGE` · Data `users.cat_color` CHECK). **기존 사용자 초기값 = 없음(null)** — 기본색을 백필하지 않고 온보딩에서 다시 고른다. A24 로 1.x 데이터는 이관하지 않으므로 승계 대상이 사실상 없다 | 확정 | 조재영 | 「권장안 승인」(2026-09-19 N27). ID 출처: `app/app-dev/src/services/model.ts` `colors`·`colorNames`, 자산 `app/app-dev/src/constants/art.ts` `avatar/<색>` 6종. 정책 문서의 「원본 샘플 3종으로 6개를 지어내지 않는다」를 앱 카탈로그로 해소. 구현 GROMO-1945 | `account/policy.md` Q03 · `account/low-level-design.md` §2.2·§2.3 · `island-management/low-level-design.md` 주민 목록 |
| 2026-09-19 | 계정-Q04 | **온보딩 완료 = 유효 name 과 catColor 를 «모두» 저장.** 기존 `isNewUser` 는 매핑하지 않는다. 색이 없는 기존 행은 이름이 있어도 미완료이고 온보딩에 재진입한다(A24 — 2.0 은 빈 상태로 시작해 승계는 최소). 신규 PATCH `/me` 와 legacy `POST/PATCH /api/v1/users/me` 가 같은 판정 함수로 변경 전·후를 비교해 false→true 일 때만 `user.onboarded` 를 같은 TX outbox 에 적는다(대상 `SCORE`=score-events, 랭킹 소비자 전까지 내구 보류) | 확정 | 조재영 | 「권장안 승인」(2026-09-19 N27). 권고 원문: `account/policy.md` Q04 「신규 사용자는 유효 name과 catColor를 모두 저장하면 완료」. 구현 GROMO-1945 | `account/policy.md` Q04 · `account/low-level-design.md` §2.2·§2.3 · `server/data-api/.../OnboardingCompletion.java` |
| 2026-09-19 | N28 | **`missingFragments` 를 화면 계약에 정식으로 올린다.** 도메인 GET 이 아직 없는 조각은 명시 `null` + `data.missingFragments`(조각 이름 문자열 배열)로 알리고, 빠진 조각이 없으면 키를 생략한다(빈 배열 아님). 의미는 「서버 미구현이라 비어 있음」 — 검증된 비적용인 `…Availability`(`facility_locked`·`host_only`·`none`)와 구분하며 missing 조각의 availability 는 `null` 이다. 앱은 「준비 중」으로 그린다. 도메인 GET 이 머지되면 같은 PR 에서 이름을 뺀다 | 확정 | 조재영 | GROMO-1897 이 도입(PR #822·#823·#826·#829·#835, `ScreenReadUseCase.missing()`) | `bff-screens/policy.md` B27 · `bff-screens/implementation-business-api.md` §4.1·§5 · `bff-screens/low-level-design.md` §1 「missingFragments」 |
| 2026-09-19 | RC-축 | **회관 기록 3종의 날짜 축 = UTC.** 기간 `[from 00:00Z, to+1 00:00Z)`, 측정 날짜도 UTC 버킷. `timezone` 은 생략 또는 `"UTC"` 만(`Asia/Seoul` 400). 새 측정은 새 표(`screen_time_observations`, UTC 날짜)에만 쓰고 KST 라벨인 legacy `daily_screen_time_stats` 는 읽지도 쓰지도 않는다 — 한 체인 안에서 축을 섞지 않는다 | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. 신규 기능이라 Q-6(섬 퀘스트)과 같은 규칙. 반려: KST 유지(RC-P02 원안 — D8 전환 뒤 다시 옮겨야 하고 퀘스트 회차와 9시간 어긋남), 두 값 모두 허용(같은 날짜를 두 축으로 읽게 됨) | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-D01 | **scope=me = 개인 전체(섬 무관), scope=island = 현재 활성 주민별 「이 섬」 기여(세션 시작 때 고정한 `focus_session_details.island_id`).** 떠났다 돌아온 주민의 과거 이 섬 기여는 합치고, 다른 섬 기록은 새 섬에 복사하지 않는다(물고기 장 §7 과 같은 귀속) | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. policy RC-D01 권장안 「개인 전체와 섬 기여 분리」. 반려: scope=me 를 경로 섬 기여로 한정(본인 기록인데 섬 이동 때 사라짐), 섬 합계에 주민 개인 전체 사용(다른 섬 집중이 섞임) | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-D02-기기 | **측정 기기 = 서명된 로그인 세션.** PUT 의 `deviceId` 는 AT 의 `sid` 와 같아야 하고(아니면 403 `FORBIDDEN` field=deviceId), 그 세션이 살아 있어야 한다. 별도 기기 등록 endpoint 는 만들지 않는다. **같은 날 기기(세션)가 둘 이상이면 그날 값은 `minutes=null`·`unavailable`** — 병합 정책(RC-D02, 1806)이 정해질 때까지 더하지도 고르지도 않는다. 원본은 기기별로 보관하므로 정책이 정해지면 다시 계산할 수 있다 | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. D7(병합 보류·저장 축만 확정) 안에서 막히는 부분만 가장 보수적으로. 반려: 임의 deviceId 신뢰·자동 등록(RC-P09 위반), FCM 토큰 사용, 합(sum — 이중 합산), 최대·대표 1대·최신 기기(누락을 전체처럼 표시, 미승인). 알려진 한계: 같은 날 재로그인하면 그날은 기기 2대로 보여 null | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-D03 | **결측은 0 으로 채우지 않고 부분 합을 전체처럼 보이지 않는다.** 관측 없는 날짜는 series 에 없다. 기간 `totalMinutes` 는 지난 날짜(`from..min(to, 오늘 UTC)`)가 **전부** authorized 일 때만 합, 아니면 null. 기간 `measurementStatus` = 가장 최근 관측 날짜의 상태(측정 전혀 없으면 `unavailable`). 가장 최근 상태가 `denied` 면 그 전 authorized 날짜를 series 에서 뺀다(권한 회수 뒤 과거 숫자 비노출). 오지 않은 미래 날짜는 결측이 아니다 | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. policy RC-D03 권장안 + 권장안 없는 세부(상태 우선순위·denied 뒤 과거값·미래 날짜)는 가장 보수적인 안. 반려: SUM(non-null)(부분 합), 새 completeness enum(LLD 금지), denied 뒤에도 과거 숫자 표시, 미래 날짜까지 요구(진행 중인 주는 합계가 영영 null) | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-D04 | **측정 PUT 유효 창 — 설정값이 정책 revision.** `measuredAt ≥ 그 날짜 00:00Z`, `measuredAt ≤ 서버 시각 + 1분`(`island-records.screen-time.future-tolerance`), 보고 마감 = 그 날짜 끝 + 12시간 = **다음 날 12:00Z**(`…report-grace`, 섬 퀘스트 screen 유예 Q-5 와 같다). 창 밖은 422 `OUT_OF_RANGE` field=measuredAt. 마감 후 정정은 받지 않는다. PUT 은 보상·퀘스트 진행·사건을 만들지 않는다 | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. policy RC-D04 권장안 「서버 설정 revision 으로 관리, 기존 창 자동 승계 금지」. 마감을 퀘스트 판정 유예와 맞춰 「판정 뒤 값이 바뀌는 정정」이 생기지 않는다. 반려: 내기 2분 상수 복제, 오늘·어제 보상 창 승계, 무기한 과거 보고 | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-D05·범위 | **legacy·정산 없는 세션은 넣지 않는다.** 집중 통계는 v0.3 세션(상세·구간 행이 있는 것) 중 진행 중(ACTIVE·PAUSED)·정상 완료(COMPLETED)만 — ABANDONED·MEMBERSHIP_LOST 와 구간 없는 legacy 세션은 뺀다. 섬 scope 는 **현재 활성 주민만**(과거 주민 비노출). 완료 기록(records)은 조회 범위에 양의 몫이 있는 완료 세션만 | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. policy RC-D05 권장안 「추측 백필 금지」. 세션 lifecycle 집합은 GROMO-1924 중복 적립 검사와 같다. 반려: legacy 세션 포함(ACTIVE 구간 없음 — 지어낸 값), 과거 주민 포함(노출 확대) | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-P12-적용 | **타인 개인정보 스냅샷을 만들지 않는 설계로 lifecycle 잠금 gate 를 해소.** scope=island 는 섬 정원(최대 15)이 페이지(30)보다 작아 커서를 발급하지 않는다(들어온 커서 400). scope=me 스냅샷(`focus_statistics_snapshots`, 15분)은 **요청자 본인 기록만** 담고 탈퇴 TX 에서 지운다. 커서는 Business HMAC 서명(스냅샷 id·offset + 사용자·섬·기간 지문) — 남의 스냅샷은 소유자 조건으로 못 읽는다(409 `CURSOR_EXPIRED`) | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. LLD §3 의 공통 lifecycle 잠금·전 사용자 역색인은 「스냅샷에 복사된 타인 PII」를 지키려는 장치 — 복사본이 없으면 필요 없다. 반려: 섬 scope 페이지 + 전 사용자 역색인·배타 잠금(1769 범위 대비 과대), asOf 만 남기고 현재 DB 로 재생성(LLD 금지) | `island-records/policy.md` RC 결정 표 |
| 2026-09-19 | RC-화면 | **도서관 화면의 기록 조각 = 이번 UTC 주(월~일)·scope=me.** 화면은 query 를 받지 않으므로 기본 기간·범위를 서버가 정한다. 집중 조각의 `nextCursor` 는 도메인 GET 이 이어받는다(B10). `statisticsAvailability` = `available` | 확정 | 조재영 — 「권장안 승인」 | 2026-09-19 N26 소유자 결정(RC-D01~D04 는 설계 문서 권장안, 권장안 없는 곳은 가장 보수적인 안 — 노출 최소·이중 합산 없음·지어낸 값 없음). GROMO-1769 구현 PR. 가장 좁은 노출(본인 기록)·주/월 전환의 첫 화면. 반려: scope=island 기본(첫 화면부터 타인 기록 노출), 월 기본(주/월 중 더 긴 조회) | `island-records/policy.md` RC 결정 표 |
| 2026-09-20 | EM-휴식 | **휴식(PAUSED) 중에도 응원을 보낼 수 있다.** 응원 자격은 「그 섬의 본인 **진행 중**(active·paused) 집중 세션」이고, 거절되는 것은 완료·포기·남의 세션·다른 섬·비주민이다 — 휴식은 같이 낚시에서 빠진 상태가 아니라 모닥불에 앉은 상태라, 쉬는 사람이 집중하는 사람을 응원하지 못하면 기능의 절반이 사라진다. 종전 LLD 의 「active 만」을 뒤집는다. 판정 정본 `GET /internal/islands/{id}/focus-members` 는 필터가 `[ACTIVE, PAUSED]` 라 휴식자를 `status:"paused"` 로 이미 싣고 있어 **새 표면이 필요 없었다**. 함께 확정: 표시 TTL **3초**, 빈도 **사용자×섬당 3초 창 1건**, 검사 순서 형식→인가→빈도 | 확정 | 재영님 | GROMO-1765 구현 중 LLD:105 와 티켓 본문 충돌을 올림 | `focus-rest-session/{prd,policy,high-level-design,low-level-design}.md` · `realtime-events/low-level-design.md` · `server/realtime/**` |
| 2026-09-20 | D5-적립 | **집중 보상은 «종료 정산」이 아니라 «유효 집중 60초마다 섬 적립»이다(GROMO-1990).** 매분 도는 서버 틱이 그 세션의 순수 집중 초를 보고 찬 60초마다 1마리를 섬 통장에 넣고, **finish 는 추가로 지급하지 않는다** — 마지막 틱 이후의 자투리는 버린다. 산식·상한(60초/1마리, 주민·섬별 하루 480)과 귀속(섬 통장 100%·개인 0)은 D5·D5-귀속-개정 그대로다. **하루 상한의 창 = UTC 날짜**(D8 확정 적용 — 이 축은 전환 대기가 아니라 «신규 축이라 처음부터 UTC», 퀘스트 Q-6·회관기록 RC-축과 같다). 자정을 걸친 60초는 끊기지 않는다(워터마크가 초 단위라 누적되고, 적립일은 그 분이 «찬» 시각의 UTC 날짜다 — **틱이 돈 시각이 아니다**). **틱이 밀려 여러 분을 한 번에 처리하면 분마다 「찬 시각」으로 날짜를 갈라 각 날의 상한을 따로 본다** — 한 건으로 접으면 여러 날의 분이 하루 상한 하나로 판정돼 나머지가 소실된다. 섬 원장에도 **분마다 한 줄**을 남긴다(금액은 언제나 1) — 접는 것은 가계부 조회뿐이다(가계부-묶음). 상한에 걸려 못 받은 몫은 **이월하지 않는다**. ⚠️ 이 전환은 「**끝난 상태 = 받은 적 없음**」이라는 종전 전제를 깬다 — 진행 중에 이미 받은 세션이 나중에 강퇴·포기로 끝날 수 있으므로, 구 앱 오프라인 업로드의 겹침 판정(1924 선행 조건 #3)은 **종료 상태가 아니라 적립 원장(실제로 받았는가)** 으로 본다. 목표 시간(`targetMinutes`)은 이로써 **선택 입력**이 된다 — 보상이 목표가 아니라 시간에만 걸린다 | 확정 | 조재영 | 2026-09-20 조정자 세션(GROMO-1990 구현 중 미결 2건 확인). **반려한 대안**: ① 자정에 걸친 분을 날짜별로 쪼개 배분(일 집계 `activeSecondsByDate` 방식) — 상한 두 개에 반 마리씩 걸려 「60초당 1마리」가 깨진다 ② 상한 초과분 이월 — 480 상한의 의미가 사라지고 자정에 몰아주기가 된다 ③ 하루 경계 KST — 일 집계와는 맞지만 퀘스트·회관기록의 UTC 축과 9시간 어긋나고 1930 전환 때 또 옮겨야 한다 | `focus-rest-session/policy.md` FR-D01·FR-P09·FR-D06 · `focus-rest-session/low-level-design.md` §2 finish·§3·**§5.2 단계 활성화 3단계** · 구현 GROMO-1990(V82 `focus_reward_accruals`(+기존 정산분 이관)·`focus_session_details.rewarded_seconds`). **운영**: 분당 적립 «크론»은 `focus.reward.accrual-enabled` 기본 꺼짐 — 혼합 버전 배포에서 새 크론과 옛 `finish` 가 다른 멱등 키로 이중 지급하므로 배포 수렴 뒤에 켜고, 롤백은 flag 를 먼저 끈 뒤에 한다. 꺼져 있어도 `finish` 가 적립을 확정해 사용자는 손해 보지 않는다. 그 창에서 «옛» finish 가 만드는 정산은 V82 의 트리거가 적립 원장으로 옮겨 상한·누적이 비지 않게 한다. **목표 없는 세션(nullable `target_minutes`)은 두 서비스 전량 배포 뒤에 `focus.session.start-enabled` 를 켠다** — 옛 독자가 null 을 못 읽는다 |
| 2026-09-20 | FR-D03-기지급 | **강퇴 시 이미 적립된 물고기는 회수하지 않는다.** FR-D03 「강제 종료·미정산」은 유지하되, 분당 적립 전환(D5-적립)으로 그 뜻이 **「마지막 틱 이후의 자투리를 주지 않는다」**로 바뀐다 — 강퇴 전까지 틱이 섬 통장에 넣은 몫은 그대로 섬의 것이다. 정산 행(`focus_settlements`)은 여전히 만들지 않는다 | 확정 | 조재영 | 2026-09-20 조정자 세션. 근거: 섬 통장은 공동 재화라 이미 건설·상점에 쓰였을 수 있고, 되돌리면 남의 지출까지 무너진다. **반려한 대안**: 음수 원장 기입으로 회수(`focus-revoke:<session>`) — 잔액 부족·완공된 건설 처리 정책이 함께 필요하고 「강퇴당한 사람 때문에 섬 잔액이 줄어든다」는 새 문제를 만든다 | `focus-rest-session/policy.md` FR-D03 · 구현 GROMO-1990 |
| 2026-09-20 | 가계부-묶음 | **공동 가계부는 집중 적립(`contribution`)을 «KST 하루»로 접어 보여준다.** 분당 적립으로 원장이 주민 1명당 하루 최대 480행이 되지만 **원장 자체는 건별로 남긴다**(감사 추적) — 접는 것은 조회뿐이다. 가계부 한 줄에 `entryCount`(접은 행 수)·`groupedUntil`(그 하루의 마지막 기입 시각)이 붙고, 접히지 않은 사유는 `entryCount=1`·`groupedUntil=createdAt` 이다. 묶음 줄의 `id`·`createdAt` 은 그 하루의 **첫** 기입 것이라 최신순 keyset 커서가 그대로 이어진다 | 확정 | 조재영 | 2026-09-20 조정자 세션. 묶는 축이 KST 인 것은 이 조회의 창(`month`)이 이미 KST 달력 월이라서다 — 상한의 UTC 축과는 다른 체인이다. **반려한 대안**: ① 원장 자체를 (세션,일) 한 행으로 누적 — 어느 분에 얼마가 들어왔는지 추적이 사라지고 이미 보여 준 행의 금액이 나중에 바뀐다 ② 묶음을 별도 배열로 분리 — 앱이 두 목록을 날짜로 섞어야 해서 화면 작업이 커진다 ③ 그대로 두기 — 한 달 수천 줄 | `internal/dto/IslandLedgerPageView` Entry(+`entryCount`·`groupedUntil`) · 구현 GROMO-1990. **공개 표면 연결·화면 계약은 GROMO-1786 몫** |
| 2026-09-21 | RK-주 | **주간 섬 랭킹의 주 = UTC 일요일 00:00Z ~ 다음 일요일 00:00Z.** 요일은 기획 정본(「주간 랭킹은 매주 일요일 00시에 초기화한다」), 시간대는 결정 D8 의 UTC 축 — 기획 정본의 「남아 있는 구현 세부 기준」에 있던 「일일·주간 초기화에 적용할 시간대」 미결이 이 결정으로 닫힌다. 최근 신규 축(섬 퀘스트 Q-6·회관기록 RC-축·집중 적립 D5-적립)이 모두 UTC 를 골랐으므로 같은 축이다. 1.x 리그의 KST 월요일(`LeagueWeek`)은 살아 있는 정산 축이라 건드리지 않고 신규 `RankingWeek` 만 둔다. **종전 `island-rankings/policy.md` RK-P01 「KST 월요일 + ISO week-year」는 폐기** | 확정 | 조재영 | 2026-09-21 조정자 세션(GROMO-1997 착수 확인) | `island-rankings/{policy,prd,high-level-design,low-level-design}.md` · `RankingWeek` · 구현 GROMO-1997 |
| 2026-09-21 | RK-주-키 | **주 식별자는 ISO `YYYY-Www` 가 아니라 주 시작일 `YYYY-MM-DD`(UTC 일요일)다.** ISO 주차는 «월요일 시작»이 정의의 일부라(종전 RK-P01·LLD:83 이 그 전제였다), 시작 요일만 일요일로 바꾸고 표기를 그대로 두면 `2026-W37` 이 ISO 가 말하는 7일과 **다른 7일**을 가리킨다 — 앱·서버·로그가 같은 문자열로 서로 다른 주를 뜻하는 가장 조용한 버그다. 주 시작일은 자기 자신이 경계를 말하므로 해석 규칙이 없고 「53주차가 없는 해」 같은 달력 예외도 사라진다. 형식 오류 400, 일요일 아님·미래 주 422 | 확정 | 조재영 — 구현 판단(RK-주 의 따름 결정) | GROMO-1997. 검증 `RankingWeekTest` 가 ISO 경계와의 불일치 자체를 못박는다. **반려**: `YYYY-Www` 를 일요일 시작으로 재정의(같은 이름이 다른 주를 뜻하게 됨 — LLD 가 명시적으로 금지) | `island-rankings/policy.md` RK-P01-키 · `low-level-design.md` §2 |
| 2026-09-21 | RK-분모 | **평균의 분모 = 그 섬 전체 주민 수, 산정 시점은 「주가 끝난 시점」으로 동결.** 나중에 누굴 내보내도 지난주 순위가 안 바뀐다 — 동결하지 않으면 **주민을 강퇴하는 것만으로 지난 주 평균이 오른다**. `group_members` 는 (user, group) 한 행이고 `left_at` 이 없어 「그 주에 몇 명이었나」를 사후 복원할 수 없으므로 표로 적는다(**migration V85 `island_weekly_member_counts`**, 주 마감 크론 + `ON CONFLICT DO NOTHING` 멱등). 진행 중인 주는 동결할 것이 없어 지금 인원으로 나눈다. **끝난 주에 동결 행이 없는 섬은 랭킹에서 «빠진다»** — 「없으면 지금 인원으로」 대체 경로를 두면 조작이 그대로 살아나므로, 조용히 틀린 순위보다 빈 순위를 택한다. **분자는 복제하지 않는다**(집중 정본이 남아 있어 언제든 다시 합칠 수 있고, 복제하면 정본과 어긋날 자리가 생긴다) | 확정 | 조재영 | 2026-09-21 조정자 세션. 기획 정본 「평균 = 해당 섬에서 집중한 시간 합계 ÷ 해당 섬 전체 주민 수. 다른 섬에서 집중한 시간은 제외한다」. 기획 정본 미결 「랭킹 집계 중 가입·탈퇴에 따른 분모 산정 시점」이 이 결정으로 닫힌다 | `island-rankings/policy.md` RK-D01·RK-D01-결손 · V85 · `schema.dbml` |
| 2026-09-21 | RK-순위 | **동점은 공동 순위**(1,1,3 — SQL `RANK()`, 다음 순위는 건너뛴다)이고 **끝난 집중만 반영한다**(진행 중 세션은 정렬·표시·`myRank` 어디에도 없다). `myRank` 는 「나보다 앞선 수 + 1」로 **전체 모집단**에서 정의하며, 목록이 상위 N 으로 잘려도 자기 섬 순위를 받는다. 평균은 **정수로 내린 값이 곧 정렬 키**다 — 표시값과 순위 기준이 달라 「화면엔 같은 숫자인데 순위가 다른」 줄이 생기지 않게. 섬 귀속은 세션 시작 때 고정한 `focus_session_details.island_id`(2026-09-19 RC-D01 과 같은 축) | 확정 | 조재영 | 2026-09-21 조정자 세션. 종전 `LeagueService:157` 의 `i+1` 은 공동순위 정책이 아니라 재사용하지 않는다 | `island-rankings/policy.md` RK-D02·D03·D05·D06 |
| 2026-09-21 | RK-스냅샷 | **섬 간 랭킹에는 불변 snapshot 저장소·전 사용자 역색인·공통 lifecycle 잠금을 만들지 않고, 페이지도 만들지 않는다.** 그 장치는 전부 「표에 복사된 타인 PII」를 지키려는 것이었는데, 주민 랭킹 폐지(B23·B15) 후 이 응답에는 **섬 이름과 평균 초밖에 없다**. 2026-09-19 RC-P12-적용이 회관 기록에서 내린 같은 판단(「복사본이 없으면 필요 없다」)을 따른다. 페이지 대신 상위 `limit` 개(기본 30·최대 100) + `myRank` 를 주므로 「움직이는 집계를 keyset 으로 넘기다 행이 빠지거나 겹치는」 문제가 성립하지 않는다. `nextCursor` 는 언제나 null 이고 들어온 커서는 400 `INVALID_CURSOR` 다 | 확정 | 조재영 — 구현 판단 | GROMO-1997. 선례 RC-P12-적용(회관 기록 scope=island 도 커서를 발급하지 않는다). **미결로 남긴 것**: `myRank` 를 공개 계약에 올린 것은 원본 JSON 에 없던 추가라 기획 확인이 필요하다(policy RK-D10) | `island-rankings/policy.md` RK-D07·D08 · `low-level-design.md` §4 |
| 2026-09-21 | 재화-단일 | **재화는 섬 단위 하나뿐이다 — 개인 물고기 지갑을 두지 않는다.** 기획 정본 「재화는 물고기 하나이며 섬 소유다. 개인 물고기 지갑은 없다」를 서버 정본이 그대로 따른다. **D1·D1-재확인(개인 지갑 + 섬 통장 둘 다 유지, 충돌은 기획을 개정해 해소)을 대체한다** — 해소 방향이 뒤집혔다: 기획을 고치는 것이 아니라 서버가 기획을 따른다. 재화의 소유 축은 섬 하나이고, 적립(집중·황금 물고기·퀘스트)도 지출(건설·상점·축음기)도 전부 `island_wallets` 한 곳이다. `personal_share_percent` 는 **0 에 고정**하고 올리는 경로를 두지 않는다(아래 개인적립-차단). D5-귀속-개정(섬 통장 100%)·SH-재화(상점은 섬 통장만 차감)·Q-1(퀘스트 보상 섬 통장 100%)은 이 결정과 같은 방향이라 **그대로 유효**하며, 이 행은 그 셋이 예외 없이 전부임을 못 박는다. ⚠️ **소유권은 이 결정의 대상이 아니다** — 기획 정본 「개인 의상·장신구는 구매한 주민의 보유품이며 탈퇴해도 유지한다」대로 `owned_products.owner_type='user'`·`catalog_assets.owner_type`·`shop_orders.owner_type`·`inventory.updated ownerType=user` 는 남는다. 재화가 섬 단위라는 사실이 소유권까지 섬 단위로 만들지 않는다. 주민별 누적 획득 기록(`island_construction_contributions`)과 개인 집중 기록도 개인 축 그대로다 — 기록은 재화가 아니다 | 확정 | 조재영 | GROMO-1989 본문 「planning-document 의 섬 소유 물고기 하나를 기준으로 서버의 개인 지갑 활성화 방향을 대체한다. 기존 결정 이력은 보존한다」. 정본: `OneOrThree/planning-document` `policy-2026-09-14.md` 「물고기 재화와 기록」·「상점·축음기·소유권」 절. 코드 실측(2026-09-21): 적립은 `IslandWalletService.contribute`, 지출은 `debitForShop`·`CONSTRUCTION_DEBIT` 뿐이고 `FishWalletService.credit` 은 `personal_share_percent > 0` 일 때만 닿는 휴면 경로다 — **구현은 이미 이 결정대로 돌고 있고 문서·스위치만 뒤처져 있었다** | `focus-rest-session/{policy,low-level-design,prd}.md` · `island-shop/{policy,low-level-design,prd}.md` · `island-construction/{policy,low-level-design}.md` · `realtime-events/{prd,low-level-design}.md` · `island-management/high-level-design.md` · `api-platform/low-level-design.md` · `bff-screens/low-level-design.md` |
| 2026-09-21 | 개인적립-차단 | **`personal_share_percent` 를 올리는 경로를 열지 않는다 — 조정 주체는 「없음」이다.** 운영 API·관리 화면·설정 파일로 이 값을 바꾸는 수단을 만들지 않는다. `focus_reward_policies` 는 추가만 하는 표이고 새 revision 은 Flyway 마이그레이션으로만 들어가므로, **`personal_share_percent > 0` 인 revision 을 넣는 마이그레이션을 쓰지 않는다**가 차단의 전부다(코드 상수·CHECK 를 새로 넣지 않는다 — 0..100 CHECK 는 V67 그대로 두고 값으로 지킨다). **재검토 주기: 정기 재검토 없음.** 「개인 돈벌기」 기획이 실제로 생기면 그때 이 로그에 새 결정 행을 먼저 쓰고, 그 행이 없는 한 어떤 구현자도 값을 올리지 않는다 — 값 하나로 재화 축이 바뀌므로 코드 리뷰가 아니라 결정 로그가 게이트다. **기존 테이블 처리 조건**: `user_fish_wallets`·`focus_settlements.personal_fish_added`·`focus_reward_policies.personal_share_percent` 는 **지금 지우지 않는다**. 삭제는 ① dev·prod 에서 `user_fish_wallets` 전 행 `balance = 0` 과 `sum(focus_settlements.personal_fish_added) = 0` 을 실측하고 ② 아래 재화-단일-호환의 공개 필드 호환 창이 닫힌 뒤 ③ 별도 조사·마이그레이션 티켓에서만 한다. 데이터 조사 없는 DROP 금지 | 확정 | 조재영 | GROMO-1989 완료 조건 「personal_share_percent 를 올려 개인 적립하는 경로의 제거 또는 차단 계획과 기존 테이블 처리 조건이 문서에 있다」. 실측(2026-09-21): V67 시드 revision 1 = (60, 480, 0) 한 행뿐이고 이 값을 바꾸는 코드·엔드포인트는 레포에 없다 — 남은 유일한 입구가 마이그레이션이다 | `focus-rest-session/policy.md` FR-D01 · `island-shop/policy.md` · `server/data-api/.../V67__focus_session_activation.sql` |
| 2026-09-21 | 재화-단일-호환 | **개인 지갑을 전제한 공개 필드의 유지·폐기 범위.** 지금 wire 에서 빼지 않는다 — 앱 2.0 이 읽고 있어 빼면 역직렬화가 깨진다. ① `GET /islands/{islandId}/shop/wallets` 의 `fish` = **항상 0**, `fishVersion` = **항상 null**(개인 지갑에 사건·버전 축이 없다) — **유지(호환), 앱은 그리지 않는다**. ② 같은 응답의 `villagePoints`·`villagePointsVersion` 이 유일한 잔액이고 `POST …/shop/orders` 의 `expectedWalletVersion` 은 **언제나 `villagePointsVersion`** 이다 — 「상품 currency 가 fish 면 fishVersion」 분기는 **폐기**(V74 `shop_orders_currency_check` 가 `village_points` 만 허용해 fish 통화 주문은 존재할 수 없다). ③ `FocusFinishView.allocation.personalFishAdded` = **항상 0**, `constructionFishAdded` = `earnedFish` 전부 — 보존식 `E=P+C` 는 `P≡0` 으로 유지(**필드 유지, 값 고정**). ④ `wallet.updated` 의 `ownerType=user`·`currency=fish` 조합은 **발행자가 없다** — data-api 의 유일한 발행자는 `IslandWalletEvents` 라 개인큐로 가는 지갑 사건은 나오지 않는다. realtime `EventPayloadValidator` 의 user/fish 분기는 **남겨 두되 죽은 분기**이며 「개인 지갑이 바뀌는 정산은 개인큐로」 서술은 폐기. ⑤ `shop_orders.payer_type`·`payer_user_id` 의 **CHECK 확장 계획은 폐기** — `payer_type='island' AND payer_user_id IS NULL` 이 영구 계약이다(`account/low-level-design.md` 탈퇴 파기표의 「CHECK 를 넓히는 변경이 파기 행을 함께 추가해야 한다」 단서는 그래서 발동하지 않는다). ⑥ `inventory.updated` 의 `ownerType=user` 와 `shop_orders.owner_type='user'` 는 **소유 축이라 그대로 유지**한다 — 재화 축과 헷갈리지 말 것. 필드 실제 제거는 앱이 전량 전환한 뒤 별도 티켓 | 확정 | 조재영 | GROMO-1989 완료 조건 「공개 wallet/allocation 필드의 유지·폐기·호환 범위가 decision-log.md 에 명시돼 있다」. 코드 실측(2026-09-21): `ShopService#wallets` 가 `fishVersion=null` 고정, `ShopUseCase#versionConflict` 가 `villagePointsVersion` 만 지목, `FocusSessionLifecycleService#settle` 이 `personal = earned × 0 / 100`, `IslandWalletEvents` 주석 「`wallet.updated` 사건의 유일한 발행자」 | `island-shop/low-level-design.md` §2.1·§2.4·§3 · `focus-rest-session/low-level-design.md` §1·§3 · `realtime-events/{prd,low-level-design}.md` · `api-platform/low-level-design.md` 버전 제출표 · `account/low-level-design.md` 탈퇴 파기표 |
| 2026-09-22 | 1958-정본-경계 | **rest 정본은 paused 세션의 모닥불 화면에 한정한다.** `GET /islands/{islandId}/rest-members`는 paused 세션 주민을 읽는 도메인 조회이고 `home.restMembers`는 그 응답을 소비하는 BFF 조각이다. Jira GROMO-1958 이전 설명에 있던 「세션 없는 공유 휴식」의 서버 상태·자리·시작/종료 계약은 pinned `rest-screen-spec.md` §1·§6의 제외 범위와 충돌하므로 현재 구현 계약으로 승격하지 않는다 — planning-document/`rest-screen-spec.md` 개정과 별도 티켓이 먼저다. 함께 정리: 응원은 active·paused 모두 5종·TTL 3초, 채팅은 active·paused 모두 차단, 휴식 1시간 자동 종료는 GROMO-1998의 정상 완료 경로다 | 확정(문서 정합화) | 조재영 | GROMO-1958 최신 본문 + planning-document `policy-2026-09-14.md`·`specs/rest-screen-spec.md` @ `23f417adf975283dd1caa871a3e462e965877167` · GROMO-1998 | `focus-rest-session/{policy,prd,README,high-level-design,low-level-design}.md` · `bff-screens/{policy,prd,low-level-design}.md` · `realtime-events/{prd,low-level-design}.md` |

| 2026-09-22 | RC-D02-병합 | **복수 기기 병합 = 하지 않는다(병합 없음 확정).** 같은 UTC 날짜에 기기(서명된 로그인 세션, RC-D02-기기)가 둘 이상이면 그날 값은 `minutes=null`·`unavailable` 이다 — 합산(이중 합산, 겹침을 가를 원본 없음)·최대·대표 1대(다른 기기의 누락을 전체처럼 표시)는 2026-09-19 RC-D02-기기 에서 이미 반려된 후보다. D7 의 「표시·집계는 출시 후 결정」은 「병합 없음 유지」로 닫힌다 — 병합 도입은 이 로그의 새 행이 먼저다. 원본은 기기별 불변 보관(`screen_time_observations`)이라 병합 정책이 승인되면 같은 원본으로 재계산할 수 있다. 규칙 정본은 `screentime/support/ScreenTimeDayPick` 한 곳이고 도서관 조회(RC-D03 resolver)와 퀘스트 판정(Q01-screen)이 공유한다 — 규칙 변경은 두 소비자를 한 커밋에서 바꾼다 | 확정 | 조재영 | GROMO-1806(RC-D02-기기 를 계약으로 확정하는 후속 정책). 기획 정본 「데이터 없음·권한 없음·실제 0은 구분한다」·「측정이 확인되지 않은 값은 0%로 표시하지 않는다」— 병합된 수는 어느 기기의 관측도 아니라 이 구분을 깬다. 미결로 남는 것: 측정 대상 범위·OS별 정확도·legacy projection 귀속(RC-D05 연계) | `island-records/policy.md` RC-D02·「복수 기기 규칙과 입력·응답 예시」 · `island-quests/policy.md` QQ03 |
| 2026-09-22 | RP-편지증거 | **친구 편지는 열려 있을 때 신고하고, 접수 시 서버 원문을 별도 보존한다.** 닫힌 뒤 해당 편지의 신규 신고는 불가하다. 이미 접수된 증거는 편지를 닫아도 유지한다. 신고 중 편지 닫기를 방지하고 접수 성공 시 증거 확보를 보장한다. 일반 편지의 서버 파기 시점은 별도 결정. 신고 원문 증거 기간은 후속 RP-증거기간에서 확정 | 확정 | 사용자(본 대화) | GROMO-1670 댓글 #11163의 2.0 범위 재정의 + 2026-09-22 브레인스토밍 첫 A 선택 | [신고센터 정책](character-report/policy.md) RP-편지증거 |
| 2026-09-22 | RP-일반신고 | **일반 신고는 신고자에게 해당 콘텐츠를 즉시 숨기고, 운영자 검토 후 전체 비공개·계정 제재를 결정한다.** 신고 건수만으로 전체에 자동 숨김하지 않는다. 검토 주기는 후속 RP-검토주기, 중대 위반 예외는 RP-긴급조치에서 확정 | 확정 | 사용자(본 대화) | GROMO-1670 · 2026-09-22 브레인스토밍 두 번째 A 선택. 집단 허위 신고로 정상 콘텐츠가 내려가는 문제 방지 | [신고센터 정책](character-report/policy.md) RP-일반신고 |
| 2026-09-22 | RP-차단 | **차단은 상호 편지·친구 요청을 막고, 차단한 사람의 화면에서 상대의 채팅·댓글·사용자 작성 공지를 숨긴다.** 같은 섬 소속은 유지하며 서비스 시스템 안내는 표시한다. 직접 연락만 막는 B안은 채택하지 않는다. 신고·차단 제공은 댓글 #11163에서 이미 정한 2.0 출시 전 필수 조건·스프린트 15 범위이며, 이번 A 선택은 도입 여부가 아닌 차단 효과의 범위를 확정한다. 1867-요청의 차단 제외 부분을 대체한다 | 확정 | 사용자(본 대화) | GROMO-1670 댓글 #11163 + 2026-09-22 사용자 「결정로그 남기고 A로하자」. 같은 섬 채팅·댓글을 통한 괴롭힘이 남는 B안의 한계. 스토어 정책이 모든 글 숨김을 명시했다는 뜻은 아님 | [신고센터 정책](character-report/policy.md) RP-차단 |
| 2026-09-22 | RP-검토주기 | **사용자에게 고정 처리 기한을 약속하지 않고 일반 신고는 주 2회 모아서 검토한다.** 긴급 신고는 별도 알림으로 우선 확인한다. 매일·24시간 이내 대응을 전제하지 않는다. 담당자·검토 요일·긴급 분류와 구체 대응 절차는 후속 결정 | 확정 | 사용자(본 대화) | GROMO-1670 · 매일 대응이 어렵다는 팀 운영 여력에 맞춰 제안한 주 2회 검토에 사용자 동의. RP-일반신고에서 남겨 둔 검토 주기 확정 | [신고센터 정책](character-report/policy.md) RP-검토주기 |
| 2026-09-22 | RP-결과안내 | **신고 접수 확인과 간단한 처리 결과를 자동 통지한다.** 운영자가 결과를 확정하면 「조치 완료 / 위반 확인 어려움」 같은 정형 문구를 전달하며, 구체적인 제재 내용은 공개하지 않는다. 개별 답변 작성을 필수로 하지 않는다. 통지 채널·최종 문구는 후속 설계 | RP-메일결과로 대체된 이력 | 사용자(본 대화) | GROMO-1670 · 접수 확인 + 간단한 결과 통지 A안 선택 | [신고센터 정책](character-report/policy.md) RP-결과안내 |
| 2026-09-22 | RP-증거기간 | **일반 신고의 원문 증거는 처리 결과 통지 후 30일 보존하고 삭제한다.** 이의제기 검토를 위한 운영 기준이며 법정 기간을 뜻하지 않는다. 진행 중인 조사·분쟁과 법적 보존 의무가 있는 사건은 별도 처리한다. 예외·처리 근거는 법률 멘토링 확인, 통지 실패·재심·메타데이터 보존은 후속 설계 | RP-메일결과의 기산점으로 개정된 이력 | 사용자(본 대화) | GROMO-1670 · 결과 통지 후 30일 보존 A안 선택 | [신고센터 정책](character-report/policy.md) RP-증거기간 |
| 2026-09-22 | RP-제재단계 | **일반 위반은 콘텐츠 조치·경고 후 반복 시 커뮤니티 일시 제한(A), 제재 후에도 반복하면 계정 전체 정지(B)로 전환한다.** A에서는 글·댓글·채팅·편지 작성을 막고 개인 집중·기록 조회는 유지한다. 운영자가 확인한 위반을 기준으로 판단하며 신고 건수만으로 자동 제재하지 않는다. 커뮤니티 기간은 후속 RP-커뮤니티기간에서 확정. 전체 정지 기간·최종 단계는 후속 RP-누진제재에서 확정. 누적 초기화는 후속 RP-누적초기화에서 확정. 중대 위반 예외는 후속 RP-긴급조치에서 확정. 오판 취소 및 제재 우회 조건은 후속 결정 | 확정 | 사용자(본 대화) | GROMO-1670 · 사용자 A 선택 후 「반복시 B로」 명시 | [신고센터 정책](character-report/policy.md) RP-제재단계 |
| 2026-09-22 | RP-커뮤니티기간 | **커뮤니티 제한(A)은 제재 적용 시점부터 7일이며 기간 만료 시 자동 해제한다.** 재위반은 운영자가 확인해 계정 전체 정지(B)로 전환한다. 신고만으로 기간을 연장하거나 B로 자동 전환하지 않는다. 계정 전체 정지 기간은 후속 RP-누진제재에서 확정 | 확정 | 사용자(본 대화) | GROMO-1670 · 사용자 「7일하는게 좋을듯」 선택 | [신고센터 정책](character-report/policy.md) RP-커뮤니티기간 |
| 2026-09-22 | RP-누진제재 | **일반 위반의 제재는 경고 → 재위반 시 커뮤니티 7일 제한 → 다시 위반하면 계정 전체 30일 정지 → 재위반 시 영구 정지 순서로 강화한다.** 7일·30일 제한은 적용 시점부터 계산하고 만료 시 자동 해제한다. 영구 정지는 자동 해제하지 않는다. 운영자가 확인한 별도 재위반을 기준으로 하며 중복 신고로 단계를 올리지 않는다. 누적 초기화는 후속 RP-누적초기화에서 확정. 중대 위반 예외는 후속 RP-긴급조치에서 확정. 이의제기는 후속 결정 | 확정 | 사용자(본 대화) | GROMO-1670 · 사용자 「A하다가 B」「제재 기간이 늘어나다가 마지막에 B」 선택 | [신고센터 정책](character-report/policy.md) RP-누진제재 |
| 2026-09-22 | RP-누적초기화 | **제재 해제 후 6개월간 재위반이 없으면 일반 위반의 누적 단계를 초기화한다. 영구 정지는 제외한다.** 초기화는 제재 단계에 대한 결정이며 원문 증거의 30일 보존을 연장하지 않는다. 최소 제재 이력·경고만 받은 경우의 기산점·검토 중 신고 처리는 후속 설계 | 확정 | 사용자(본 대화) | GROMO-1670 · 사용자 6개월 무위반 시 초기화 A안 선택 | [신고센터 정책](character-report/policy.md) RP-누적초기화 |
| 2026-09-22 | RP-긴급조치 | **아동 성착취·구체적 위해 협박 등은 긴급 알림으로 우선 검토하고, 운영자가 긴급성을 확인하면 즉시 콘텐츠 전체 비공개·계정 임시 정지한다. 조사로 중대 위반이 확정되면 일반 제재 단계를 건너뛰고 영구 정지한다.** 신고 사유 선택만으로 자동 정지하지 않는다. 담당자·알림 수단·임시 정지 재검토·오판 복구·법적 신고 및 보존 의무는 후속 확정 | 확정 | 사용자(본 대화) | GROMO-1670 · 사용자 중대 위반 예외 절차에 동의 | [신고센터 정책](character-report/policy.md) RP-긴급조치 |
| 2026-09-22 | RP-법률확인 | **관련 법률 멘토링 확인 결과가 아직 없다.** ~~공식 확인 전 법적 절차를 확인 대기로 남긴다~~ → 후속 RP-자율구체화에 따라 공식 근거 조사·실행 절차 작성 완료. 회사별 적용과 개인정보 처리 근거 확인은 계속하며 RP-긴급조치의 제품 결정과 구분한다 | 확인 중 | 사용자(본 대화에서 결과 없음 확인) | GROMO-1670 · 법률 멘토링 확인 결과 질문에 사용자 「없음」 답변 | [신고센터 정책](character-report/policy.md) RP-법률확인 |
| 2026-09-22 | RP-자율구체화 | **멘토링·추가 질의 응답을 기다리지 않고 가능한 정책 구체화와 공식 근거 조사를 진행한다.** 기존 확정 RP는 유지하고 새 제품 세부안은 D1~D7 초안으로 구분한다. 공식 기관·법령 조사와 후속 범위·검증 기준을 작성하며, 실제 회사별 적용 사실·담당 실명은 추정하지 않는다 | 확정(진행 방식) | 사용자(본 대화) | GROMO-1670 · 사용자 「대기하지말고 할 수 있는 만큼하자」 | `character-report/{policy,legal-response,follow-up-scope}.md` |
| 2026-09-22 | RP-신고차단연계 | **신고 화면에 「이 사용자도 차단」 선택 항목을 기본 미선택으로 제공한다.** 신고만 하면 해당 콘텐츠를 숨기고, 선택 시 사용자 차단 효과까지 함께 적용한다. 기존 친구·편지 처리 등 D3의 다른 세부안은 별도 제안으로 유지한다 | 확정 | 사용자(본 대화) | GROMO-1670 · 신고와 차단 연결 방식 질문에 사용자 A 선택 | [신고센터 정책](character-report/policy.md) RP-신고차단연계 |
| 2026-09-22 | RP-통합증거 | **모든 2.0 텍스트 신고는 접수 시 서버 원문의 별도 증거 사본을 확보하고 신고자에게 해당 콘텐츠를 숨긴다.** 공지·댓글·섬 채팅·열려 있는 받은 편지·닉네임·섬 이름/소개에 적용한다. 원본 수정·삭제와 독립해 보존하고 일반 사건은 처리 결과 통지 후 30일에 파기한다. 조사·분쟁·법적 보존 예외는 RP-증거기간을 따른다. 이 기간은 신고 증거 사본 기준이며 일반 콘텐츠 전체의 보관 기간을 바꾸지 않는다. 신규 이미지 저장소는 도입하지 않으며 텍스트 증거 저장·접근 통제·파기와 개인정보 처리방침 반영을 부수 작업에 포함한다 | 확정 | 사용자(본 대화) | GROMO-1670 · 전체 텍스트 서버 원문 사본 방식 제안에 사용자 「신고자에게는 안보이고」「30일 보관」 확인 후 「ㅇㅇ 그렇게하자」 동의 | [신고센터 정책](character-report/policy.md) RP-통합증거 |

| 2026-09-24 | RP-메일정본 | **신고 사건·원문 사본의 자체 DB를 만들지 않고, 서버가 대상 권한·원문을 확인해 전용 운영 메일함으로 보낸다. 메일함이 신고 기록과 증거의 정본이다.** RP-통합증거의 원문 확보·원본 변경과 독립된 보존·결과 통지 후 30일 파기 원칙은 유지하며 저장 장소만 개정한다. 실제 CSAM 의심 이미지·영상은 메일에 첨부하거나 자동 수집하지 않는다. 발송 성공 판정·반송/중복·처리 상태·결과 통지·보존 예외·파기 기준은 이메일 접수 계약에 정리했으며 제공자 구현 가능성은 후속 검증 | 확정(저장 방향), 제공자 검증 대기 | 사용자(본 대화) | GROMO-1670 · “서버가 굳이 기록하지 않고 이메일로 넘길 수 있도록” 제안 후 “ㅇㅇ 반영” | [신고센터 정책](character-report/policy.md) RP-메일정본 · [후속 범위](character-report/follow-up-scope.md) |
| 2026-09-24 | RP-차단-정합 | **GROMO-1975의 차단 API와 친구 목록·검색·받은 편지함 표시 필터는 RP-차단의 일부 구현이다.** 종전 `차단-도입` 행의 “표시 필터”는 당시 구현 범위를 뜻하며, 2.0 최종 범위인 양방향 편지·친구 요청 제한과 차단자 화면의 채팅·댓글·공지 숨김을 축소하지 않는다. 앱 차단 UI는 기존 GROMO-1976, 신고 UI는 GROMO-1673에서 맡되 현재 정책에 맞춰 갱신한다 | 확정 정책의 최신 `main` 정합화·구현 잔여 | 사용자(본 대화) | GROMO-1670 · 현재 백엔드가 출시 최소 조건을 만족하면 따른다는 피드백을 검사했으나 API·일부 목록 필터만 확인. Jira 티켓 재확인 | [신고센터 정책](character-report/policy.md) RP-차단 · [후속 범위](character-report/follow-up-scope.md) |
| 2026-09-24 | RP-표시차단-검토 | **차단 목록을 앱이 대조해 편지·요청을 화면에서 숨기는 방안을 검토했다.** GROMO-1975의 `GET /blocks`로 가능하지만 API 직접 호출, 실시간 전송과 알림의 서버 누출을 막지 못한다. 아래 RP-서버차단 결정으로 이 방안은 최종 2.0 계약에서 제외한다 | 검토 이력, 대체됨 | 사용자(본 대화) | GROMO-1670 · “블락 리스트를 받을 수 있나? … 프론트 단에서 해당 리스트와 대조” 이후 최신 지시로 변경 | [신고센터 정책](character-report/policy.md) RP-차단 |
| 2026-09-24 | RP-서버차단 | **서버가 차단 관계를 판정해 양방향 편지·친구 요청의 새 쓰기를 거절하고, 차단자에게 상대의 채팅·댓글·사용자 공지·관련 알림을 전송하지 않는다.** 목록·상세 ID 조회·실시간·푸시에도 적용한다. 앱에는 차단 조작 UI를 제공한다. GROMO-1975는 일부 구현일 뿐이며 직접 연락·읽기/실시간 서버 잔여 범위는 새 티켓으로 분리한다 | 확정, RP-표시차단-검토 대체 | 사용자(본 대화) | GROMO-1670 · “UI 숨김말고 그냥 백에서 안보내는걸로 다시 바꾸자” | [신고센터 정책](character-report/policy.md) RP-차단 · [후속 범위](character-report/follow-up-scope.md) |
| 2026-09-24 | RP-미디어범위 | **Catus 2.0에는 사용자 이미지·파일의 영구 저장과 타인 조회·공유를 넣지 않는다.** 기존 `POST /api/v1/character/moderation`은 검사 목적 PNG 입력을 받아 외부 검사 서비스로 전송하므로 활성 이미지 처리 경로이며 개인정보·오남용·불법 자료 유입 대응을 출시 전에 검증한다. 신고센터 구현은 서비스 안의 텍스트 UGC와 외부 자료를 가리키는 텍스트 신고에 집중한다. CSAM 문서 완료조건은 텍스트/외부 링크를 통한 의심 신고의 대응 절차로 충족하고, 새 이미지 신고 첨부·증거 저장 시스템을 2.0 선행 조건으로 만들지 않는다. 향후 미디어 공유를 열 때 별도 정책·구현·운영 게이트를 둔다 | 확정(2.0 범위), 기존 API 사실관계 보정·미래 미디어 게이트는 후속 설계 | 사용자(본 대화) | GROMO-1670 · “지금 이미지나 파일 올리는거 빠짐”, “다시 계획해봐” · 최신 main 250a13f5 코드 대조와 활성 검사 API 재확인 | [신고센터 정책](character-report/policy.md) 범위·D7 · [긴급 대응](character-report/legal-response.md) · [후속 범위](character-report/follow-up-scope.md) |
| 2026-09-24 | RP-1670완료경계 | **GROMO-1670의 완료조건은 정책 문서에 증거 방식·부수 작업, 신고→조치 흐름, CSAM 의심 대응 절차, 공유 기능과의 선후 관계를 정리하는 것이다.** 후속 구현 범위를 문서에 적으면 이 조건은 충족되며 신규 Jira 티켓 생성·기존 티켓 갱신/연결·PR 병합·2.0 기능 구현은 1670을 완료하기 위한 선행 조건이 아니다. 실제 출시 작업은 이후 별도로 추적한다 | 확정(완료 판정 범위) | 사용자(본 대화) | GROMO-1670 · “문서만 완성하면 나머지 모두 후속 티켓을 만들거나 그럴필요 없는거 아님?” | [완료조건 판정](character-report/gap-review-2026-09-24.md) · [후속 구현 범위](character-report/follow-up-scope.md) |
| 2026-09-24 | RP-메일결과 | **앱에서 사건 ID·접수 확인은 제공하되 결과는 선택 입력한 확인된 이메일 주소로만 정형 답장한다.** 답장 주소가 없는 신고도 접수하며 사후 결과 통지는 하지 않는다. 앱 내 결과 수신함과 모든 신고자 자동 통지에 관한 RP-결과안내의 옛 방향을 대체한다. 일반 증거는 답장한 사건은 최초 결과 답장 발송 후 30일, 답장 주소가 없거나 발송 실패가 확정된 사건은 운영자 무연락 종결 후 30일에 파기한다. 조사·분쟁·법적 보존 예외 유지 | 확정 | 사용자(본 대화) | GROMO-1670 · “이메일 답장만 제공 — 결과 통지 정책을 변경” 선택 | [신고센터 정책](character-report/policy.md) RP-메일결과 · [이메일 접수 계약](character-report/email-intake-contract.md) |

| 2026-09-22 | 2054-메인섬-도출 | **메인 섬 자동 도출 = 가장 최근 가입 섬** — 도출과 이전이 같은 축이다. 기획 정본 `policy-2026-09-14.md` L129 「남은 섬 중 가장 최근에 가입한 섬이 메인 섬이 된다」를 고른 행이 없을 때의 도출에도 그대로 적용한다 — 종전 구현(GROMO-1971)은 «가장 먼저 가입»으로 도출해 정책과 어긋났다. 정렬 축은 «멤버십 시작 시각» `COALESCE(rejoined_at, created_at)` — 탈퇴 후 재가입한 섬은 되살린 시각이 최근 가입으로 친다(재가입 컬럼은 GROMO-2050/V97). 위의 1867-메인섬 행과 어긋나지 않는다 — 그 행이 정한 것은 «이전» 규칙(탈퇴·강퇴 시 가장 최근 가입 섬으로)이고, 이 행은 «도출»을 그쪽으로 맞춘다. 명시 선택·이전으로 `user_main_islands` 에 박힌 섬은 도출보다 우선한다 | 확정 | 조재영 | GROMO-2054 — 티켓 2038 작업 중 발견한 도출-정책 어긋남. 어긋난 채면 티켓 1995 의 현재 섬 복구(`UserIslandContextRecovery`)가 사용자를 방금 나온 섬 대신 오래전 섬으로 끌고 간다 | `server/data-api/.../MainIslandService` · `GroupMemberRepository.findActiveIslandsJoinedDesc`/`countActiveJoinedAfter` · `island-rankings/policy.md` RK-D01-기준시각 근거 문구 |

| 2026-09-24 | 계정-D1-최종 | **2.0 로그인 제공자 노출은 한국과 한국 외를 나눈다.** 기기 로케일 `regionCode`가 `KR`이면 iOS는 카카오·애플·구글, Android는 카카오·구글을 노출한다. 그 밖의 지역이나 판정 불가이면 iOS는 라인·애플·구글, Android는 라인·구글을 노출한다. 게스트는 모든 행에서 제공자 버튼 아래 보조 액션으로 유지한다. Facebook·Snapchat은 이번 범위에 넣지 않는다. 적용 범위는 2.0이며 1.x `gromo`는 기존 플랫폼별 노출을 유지한다. 서버가 `gromo`와 `focuscat`을 모두 Apple 허용 audience로 검증하기 전에는 애플 버튼만 숨겨 출시하지 않고 iOS 2.0 전체 출시를 보류한다. | 확정 | 권태화 | GROMO-1967 최종 결정. 국가 판정은 스토어 국가·IP·GPS가 아닌 기기 로케일 지역 코드만 사용한다. 국가 이동으로 가입 제공자가 기본 화면에서 사라질 수 있는 한계는 수단 변경·복구를 도입할 때 재검토한다 | `account/policy.md` LOGIN-D01·로그인 제공자 노출·순서 |
| 2026-09-24 | 계정-D1-국가이동 | **국가 이동에 따른 카카오·LINE 재진입 잠금은 현재 한계로 수용한다.** 계정당 로그인 수단 하나 정책과 지역별 제공자 노출을 유지하며, 계정 수단 변경·복구 경로는 이번 범위에 넣지 않는다. 향후 변경 시 LOGIN-D01과 로그인 표를 같은 개정에서 수정한다. 이 결정은 국가 이동 축이며, iOS Apple 가입자의 Android 이동을 정한 `계정-Q07`과 별개다. | 확정 | 권태화 | GROMO-1967. 게스트 승격은 기존 계정 복구가 아니므로 대체 수단으로 세지 않는다 | `account/policy.md` LOGIN-D01·로그인 제공자 노출·순서 |

## 이 로그가 정하지 않은 것 (2026-09-24 기준 남은 미결)

- 신고센터(1670): 기존 핵심 RP 결정에 2026-09-24 메일 정본·선택적 결과 답장·차단 구현 경계 개정을 더했다. [정책](character-report/policy.md)의 D1~D7은 여전히 후속 설계 초안이며, [이메일 접수 계약](character-report/email-intake-contract.md)은 구현 가능성·제공자 파기 범위를 검증해야 한다. CSAM 경로는 [공식 근거와 대응 초안](character-report/legal-response.md), 구현 분할은 [후속 범위](character-report/follow-up-scope.md)에 있다. 운영 실명·연락처와 실제 구현은 완료로 간주하지 않는다.

- ~~「각자 몫 n빵」의 서버 정산 경로(주민별 누적 획득 기록의 저장 단위·인원 변동 시 재계산)·공사 시간 동안의 시설 상태·건설 트리거(기획 정본은 「방장이 건설하기를 누를 때」라 적었으나 D2 가 주민에게도 열 수 있게 했다)~~ → **부분 해소(2026-09-21)**: 대상 명단·분모는 GROMO-1999(`island-construction/policy.md` P-D04), 건설 트리거는 GROMO-2000(방장만)으로 닫혔다. 잔여는 P-D02(완공 후 목표 유지/해제)·P-D04(목표 변경 시 몫 이어세기·일일 퀘스트 보상 포함 여부).
- ~~`SHARED_PURCHASE` 토글 기본값·설정 변경 API(D2), 목표 변경(PUT construction-target)이 같은 토글을 따르는지.~~ → **해소(2026-09-21, GROMO-2000·D2-개정)**: 토글 자체가 폐기됐다 — 건설(PUT·POST)은 방장만, 공동 구매·외양은 활성 주민 누구나.
- 상품 가격(S03)·기본 3곡/초기 자산(S04) — 상점 테이블은 선구축됐고(SH-테이블) 이 둘이 판매 활성화를 막는다 · 도서관 기능(1822)·~~복수 기기 병합(RC-D02 — 저장은 기기별, 같은 날 복수 기기는 null 로 보류 중)~~ → **2026-09-22 RC-D02-병합 으로 「병합 없음」 확정**(남은 미결은 측정 대상 범위·OS별 정확도·legacy 귀속)·UTC 전환 단계 계획(1930).
- **달성했지만 개인 몫을 받지 않은 채 회차가 바뀔 때의 처리** — 기획 정본의 「남아 있는 구현 세부 기준」에 미결로 올라 있다. 현재 구현은 «수령 마감(focus 다음 날 12:00Z, screen 다음 날 24:00Z)이 지나면 소멸»이고, 대안은 «마감에 미수령분을 자동 정산»이다(GROMO-1991 보고 참조).
- ~~기획 정본 「개인 물고기 지갑은 없다」·GROMO-1849 「개인 의상도 섬 물고기로」의 **개정**(D1-재확인으로 이 로그가 우선이지만 기획 문서는 아직 옛 전제) — 기획 몫.~~ → **해소(2026-09-21 재화-단일).** 기획을 고치는 대신 서버가 기획을 따른다 — 기획 정본 개정 요청은 철회한다.
- 편지: FL-결정-1·2·본문은 확정이고, **FL-형태·FL-결정-3 도 2026-09-21 에 닫혔다**(FL-형태-개정 = 기록 안 남음 · FL-결정-3 = 미확인 편지 삭제). 「1867 기획 정정」 항목도 함께 사라진다 — `policy-2026-09-14.md` 가 1867 쪽으로 정렬됐다. 남은 것은 FL-누출-수용의 대가를 나중에 되살 것인가뿐이고, 그건 미결이 아니라 **수용한 결정**이다.
- 모든 섬에서 강퇴된 사용자 처리 — GROMO-1867 「미정」. 안수빈 제안(#11094, 2026-09-16): 온보딩 화면(섬 만들래?/기존 섬 찾을래?)으로 보낸다. 미승인이며 소속 [IM-D06](island-membership/prd.md)(현재 섬 상실 복구)과 같이 정해야 한다.
- **「세션 없는 공유 휴식」의 서버 상태·자리·시작/종료·노출 계약 — 현재 정본 제외 범위다.** 채택하려면 `planning-document`의 `policy-2026-09-14.md`·`specs/rest-screen-spec.md` 개정과 별도 티켓이 먼저다(2026-09-22 1958-정본-경계).
- ~~`island-rankings/*` 의 주민 랭킹 엔드포인트 폐지 반영(B23·B15 행)~~ → **2026-09-21 GROMO-1997 에서 반영 완료.**
