# 기능별 App Store 이미지 — 카피 v2

`features-v1/`의 실제 앱 화면, 배치, 색상은 그대로 유지하고 헤드라인과 보조 문구만 변경한 두 번째 세트다.

- `group/`: 감정적 동기와 함께하는 습관 강조
- `challenge/`: 경쟁·목표 설정·약속 이행 강조
- `cutout/`: 유일성·간편함·완성의 재미 강조

재생성:

```bash
python3 .docs/app-imgs/tools/render_feature_images_v1.py --variant copy-v2
```
