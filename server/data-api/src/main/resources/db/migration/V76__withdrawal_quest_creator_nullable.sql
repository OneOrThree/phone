-- ════════════════════════════════════════════════════════════════════
-- V76 — 탈퇴 파기에 필요한 퀘스트 작성자 NOT NULL 해제 (GROMO-1952 · 계정 LLD §4)
-- ════════════════════════════════════════════════════════════════════
-- 퀘스트 정의(island_quests)는 섬 자산이라 만든 방장이 계정을 탈퇴해도 남고 매일 회차가 계속 열린다.
-- 그 연결(created_by)만 끊는다. 작성자가 없는 퀘스트의 회차는 시스템 주체로 연다(IslandQuestService).
-- 생성 writer 는 그대로 non-null 을 쓴다 — 여기서 풀리는 것은 탈퇴 파기 경로뿐이다(V75 와 같은 방식).
ALTER TABLE island_quests ALTER COLUMN created_by DROP NOT NULL;
