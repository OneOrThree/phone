node_modules/
.expo/
.env*
!.env.example
dist/
npm-debug.*
*.log
*.tsbuildinfo
*.jks
*.keystore
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
build/
.docs

# Expo가 자동 생성하는 타입 선언 (process.env.EXPO_PUBLIC_*, 에셋 모듈 등)
expo-env.d.ts

# Firebase 설정 파일 (프로젝트별 키 — 커밋 금지, 로컬/EAS secret으로 관리)
# 활성 파일 + 환경별 원본(-dev/-prod, google-services-*.json) 모두 무시
GoogleService-Info.plist
GoogleService-Info-*.plist
google-services.json
google-services-*.json
# hot-updater
.hot-updater/output
.hot-updater/log
# supabase CLI가 만드는 로컬 링크 캐시 (프로젝트 ref — 커밋 불필요)
supabase/.temp/

# Maestro E2E 실행 리포트 (scripts/e2e.sh 산출물)
.maestro/report/

# Android 환경별 빌드 자격 파일 (keystore·google-services·비번 — 커밋 금지, 별도 공유)
android/credentials/

# 로컬 모듈 안드로이드 빌드 캐시 (Gradle 산출물)
modules/*/android/.gradle/

# jest 커버리지 리포트 (npm run test:coverage 산출물)
coverage/
