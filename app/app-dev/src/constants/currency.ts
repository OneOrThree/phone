// 인게임 재화 "시간조각" — UI 표기 상수 (코드 식별자는 coin/currency 유지)
// 아이콘은 이 상수에 없다 — 이모지 ⏳가 기기·폰트마다 모양이 달라 @/components/CurrencyIcon 으로
// 전부 옮겼다(GROMO-1072). 아이콘이 필요하면 그 컴포넌트를 쓸 것.
export const CURRENCY = { label: '시간조각' } as const;
