node_modules/
.expo/
.env
.env.production
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
build/
.docs

# Expo가 자동 생성하는 타입 선언 (process.env.EXPO_PUBLIC_*, 에셋 모듈 등)
expo-env.d.ts

# Firebase 설정 파일 (프로젝트별 키 — 커밋 금지, 로컬/EAS secret으로 관리)
GoogleService-Info.plist
google-services.json
# hot-updater
.env.hotupdater
.hot-updater/output
.hot-updater/log
# supabase CLI가 만드는 로컬 링크 캐시 (프로젝트 ref — 커밋 불필요)
supabase/.temp/
